# 社内ネットワーク更改　全体サマリ

最終更新：2026-09-15 ／ 本フォルダの**入口となる資料**です。詳細は各リンク先を参照してください。

> **進捗メモ（2026-09-15）**：切替・VLAN分離・アクセス制御・**AWS拠点間VPN**は 2026-09-14 に構築・稼働確認まで完了。新OCN回線への復帰も 2026-09-15 に完了。残るのは主に「**リモートアクセスVPN（L2TP）の実装一式**（優先度低）」「So-net解約」「map-eインバウンド疎通の確認」、および後日検討の「無線LAN認証RADIUS化」。最新の進捗は [[インターネット回線更改]] のチェックリスト、経緯は [[conversations]] を参照。

---

## 1. 何をやっているか

**フレッツ光クロス（10G）＋ OCN光 IPoE 固定IP1 への回線更改**にあわせ、社内LANをVLAN分離した新構成へ移行する。

当初の資料は**HGW（XG-200KI）の存在を考慮していなかった**ため、HGW前提での設計に全面的に見直した。その過程でIPアドレス設計・アクセス制御・リモートVPN・機器configまで作成済み。

---

## 2. ファイル構成

| ファイル                                      | 内容                                            |
| ----------------------------------------- | --------------------------------------------- |
| **[[00_サマリ]]**                            | ← 本資料。全体像と残タスク                                |
| [[ネットワーク構築提案書]]                           | 機器構成・費用・VLAN設計の提案書                            |
| [[社内LANネットワーク設定]]                         | **設計の本体**。ポート結線図、IPアドレス設計、アクセス制御、リモートVPN設計    |
| [[切替・障害切り分け手順]]                           | **切替当日に手元で開く**手順書。事前準備／切替手順／疎通確認／症状別切り分け／切り戻し |
| [[インターネット回線更改]]                           | 進行管理チェックリスト                                   |
| [[現行config棚卸し]]                           | 現行RTX1200 configの引き継ぎ判定                       |
| [[config_XG-200KI]]                       | HGWのGUI設定手順                                   |
| [[config_RTX1300]]                        | RTX1300のコマンドconfig                            |
| [[config_XS508TM]]                        | コアスイッチのGUI設定表                                 |
| [[00_Claude/conversations]]                         | 決定の経緯・根拠・出典の時系列ログ                             |
| `ネットワーク図.drawio` / `.png`                 | 構成図（drawioが正本）                                |
| `config/config_1200_OCN.txt`              | 現行RTX1200のconfig（**平文の認証情報を含む**）              |
| `OCN_Hikari_Cross_RTX1300_Setup_Guide.md` | 参考資料（無修正。方式選択のみ本設計を優先）                        |

---

## 3. 確定した構成

```
[ フレッツ光クロス 10G ]
        │ 光ファイバ
        ▼
[ 10G-EPON ONU（光加入者終端装置・別体）]  ← 光を終端。UPS保護対象
        │ 10GBASE-T / CAT6A
        ▼
[ NTT XG-200KI（HGW／ONU一体型ではない）]  ← 撤去・バイパス不可
   ・Wi-Fi 常時OFF
   ・ルータ接続先設定なし（パススルー運用）
   ・LAN4のみ使用／LAN1〜3はメンテナンス時のみ
   ・LAN側 192.168.1.1/24 は動作し続ける（変更不可）
        │ LAN4 → LAN2 RJ-45（CAT6A / 10GbE）
        ▼
[ YAMAHA RTX1300 ]  ← 固定グローバルIPv4を直接保持
   ・LAN2（物理9）＝WAN
   ・LAN3（物理10）＝タグVLAN幹線（VID 10/20/30）
   ・LAN1（物理1〜8）＝保守セグメント
        │ LAN3 → Port 1（10GbE / タグVLAN）
        ▼
[ NETGEAR XS508TM（コアスイッチ）]
   Port2=開発席 / Port3=総務席 / Port4=営業・管理席
   Port5=PoEエクステンダー→BE9400 / Port6=複合機 / Port7,8,SFP+=未使用
```

---

## 4. IPアドレス設計（確定）

| 区分 | ネットワーク | GW | 備考 |
|---|---|---|---|
| **VLAN 10** 営業・総務管理 | `192.168.128.0/24` | `.1` | 現行セグメントを継続 |
| **VLAN 20** 社内開発・試験 | `192.168.64.0/24` | `.1` | |
| **VLAN 30** ゲスト専用 | `192.168.192.0/24` | `.1` | 来客のみ |
| 保守セグメント | `192.168.200.0/24` | `.1` | RTX1300 LAN1 |
| VPNクライアント払出 | `192.168.201.0/24` | － | `pp anonymous` に終端 |
| HGW管理 | `192.168.1.0/24` | `.1`（HGW） | **変更不可** |
| AWS VPC | `10.0.3.0/24` | － | 事後対応 |

**各VLAN内の範囲**：固定IP `.2`〜`.99` ／ DHCP予約 `.100`〜`.149` ／ DHCP自動 `.150`〜`.199` ／ 予備 `.200`〜`.254`（マスクは全て `/24`）

**固定IP機器**：複合機 `192.168.128.20` ／ XS508TM管理 `.2` ／ WBE710管理 `.3` ／ AKERUNコントローラは**DHCP**

---

## 5. アクセス制御（確定）

| 通信方向 | 可否 |
|---|:--:|
| VLAN10 → VLAN20 | **許可**（動的フィルタで片方向） |
| VLAN20 → VLAN10 | **拒否**（例外なし） |
| 複合機 `192.168.128.20` → VLAN20 | **拒否**（VLAN10→20許可の例外） |
| VLAN30 → VLAN10・20・保守・AWS | **拒否** |
| VLAN30 → インターネット | 許可 |
| VLAN10 → AWS `10.0.3.0/24` | 許可（**事後対応**） |
| VLAN20・30 → AWS | 拒否 |
| 保守 → VLAN10・20 | 許可（逆方向は拒否） |
| インターネット → 社内（着信） | **拒否**（インバウンド公開なし） |
| VPN業務 → VLAN10 / VPN開発 → VLAN20 | TCP 3389・22 のみ許可 |

> **VLAN間の ping は通りません。** 動的フィルタがICMP非対応のため。疎通確認はTCPで実施（→ 6章の罠④）。

---

## 6. 踏んではいけない罠（最重要）

この8点は調査・検証で確定した内容です。**知らずに設定すると当日詰みます。**

|   # | 罠                                          | 正解                                                                                                                                        |
| --: | ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- |
|   ① | **IPv6取得方式をDHCPv6-PDにする**                  | 光クロスの原則はPDだが、**XG-200KI配下は例外でRA方式**。`ipv6 lan2 address ra-prefix@lan2::<IF_ID>/64`。PDにすると**IPv6は通るがIPv4が全く通らない**（回線が生きているように見えるため切り分けが難航） |
|   ② | DNSを自動設定のままにする                             | `dhcp lan2` でHGWを参照し到達経路が生成されず名前解決に失敗。**パブリックDNSを直接指定**                                                                                   |
|   ③ | **VLAN IDに 1 を使う**                         | VID 1 は既定VLAN（ネイティブ／アンタグ）として予約。**VID 10／20／30 を使用**                                                                                       |
|   ④ | 動的フィルタでICMPを許可しようとする                       | **ICMPは指定不可**。VLAN間の ping は通らない。**疎通確認はTCPで**（`Test-NetConnection -Port 3389`）                                                            |
|   ⑤ | 片方向許可を静的フィルタだけで組む                          | **戻りパケットが落ちて通信不能**。宛先側IFの `in` で相手発を拒否し、`out` に静的 `pass` ＋ `dynamic` を置く                                                                  |
|   ⑥ | LAN側インターフェースに `secure filter out` を置く      | インターネットやVLAN20からの**戻りパケットまで落ちる**。`lan3/1`・`lan3/3`・`lan1` には `out` を設定しない                                                                 |
|   ⑦ | `ip pp remote address pool` と固定指定IPを併用     | プール側から別アドレスが払い出され**フィルタが効かなくなる**。全ユーザー固定指定・プール未使用                                                                                         |
|   ⑧ | RTX1200/1210の記事どおり `vlan port mapping` を使う | **RTX1300では従来のLAN分割機能が使えない**。`lan flexible-port` で代替（変更時は `save`＋`restart`）                                                               |

補足：**フィルタは適用順序が結果を決めます。** 複合機→VLAN20の拒否（2100）は許可（2101）より前、かつ静的に `pass` されないと動的セッションが生成されません。

---

## 7. リモートアクセスVPN（L2TP/IPsec）

主用途は**社外から自席PCへのRDP／SSH**。L2TPクライアントはVLANに所属しないため、**ユーザーごとに固定IPを払い出しレンジでフィルタを制御**。**（2026-09-15）将来性を考慮し、業務=VLAN10到達・開発=VLAN20専用到達の両方を用意**。

| アカウント | 利用者 | 区分 | 払出IP | 到達範囲 |
|---|---|---|---|---|
| `yohira` | yohira | 業務 | `192.168.201.17` | VLAN10（3389/22） |
| `t.kido` | t.kido | 業務 | `192.168.201.18` | VLAN10（3389/22） |
| `mogik` | mogik | 業務 | `192.168.201.19` | VLAN10（3389/22） |
| `iizukak` | iizukak | 業務 | `192.168.201.20` | VLAN10（3389/22） |
| `yohiradev` | yohira | 開発 | `192.168.201.33` | VLAN20（3389/22） |
| `t.kidodev` | t.kido | 開発 | `192.168.201.34` | VLAN20（3389/22） |

- 利用者4名／**アカウント6個**（開発2個は当面の暫定・運用で増減）。トンネルは**合計5本**（同時接続の目安 VLAN10=2／VLAN20=3＝容量計画。到達先はトンネルでなくアカウント払出IP＋フィルタで決まる＝解釈A）
- RDP/SSHの接続先PCは **DHCP予約（`dhcp scope bind`）** で `.100`〜`.149` に固定
- 端末側の確認が必要：**Windowsファイアウォール**のRDP受信規則が「ローカルサブネットのみ」だと `192.168.201.x` から繋がらない。Linuxは `sshd`／firewalld で `192.168.201.0/24` を許可

---

## 8. 今回のスコープと事後対応

| 項目 | 扱い | 理由 |
|---|---|---|
| 回線切替・社内LAN・VLAN・リモートVPN | **今回** | － |
| **AWS拠点間VPN** | ~~事後対応~~ → **完了（2026-09-14）** | CGW `cgw-0c4eb6ae228af69d1` は外側IPが新固定IPで再作成不要と判明。VPN `vpn-02dfe4cb7fb8d8155` 作成・RTX §11投入・BGP確立・エンドツーエンド疎通（VLAN10/20→AWS RDP/SSH）まで確認済 |
| **Web公開環境** | **事後対応** | サーバ群はAWS移行済み。新構成に合わせ別途検討（現行 `192.168.128.128` の公開は移植しない） |

**グローバルIPが `153.156.71.228`（旧）→ `124.100.212.73`（新・map-e固定IP1）に変わりました。** 外部IP許可リスト登録先の洗い出し・変更依頼は対応済み。VPN利用者6名への周知は未実施（下記残タスク）。

---

## 9. 残タスク（2026-09-15 時点）

> 進行管理の正本は [[インターネット回線更改]] のチェックリスト。以下は未完了項目のみを抜粋。
> **完了済**（参考）：切替・VLAN分離・アクセス制御・全VLANの疎通/インターネット接続、RTX1300ファームRev確認、ネットワーク図.png再書き出し、`t-yamashita`・`takebuchi` 登録対象外の確認、外部IP許可リスト登録先の洗い出し・変更依頼、HGW側設定（接続先なし・DMZ不要）確認、**AWS拠点間VPN構築・稼働確認**（すべて 2026-09-14 までに完了）。

### A. 回線・契約
| 内容                                       | 連絡先/備考           |
| ---------------------------------------- | ---------------- |
| ~~総務・営業LANを新OCN回線へ戻す~~ **完了（2026-09-15）** | －                |
| **So-netコラボ解約**（⑥）                       | **約1か月後（2026-10中旬目安）**。新OCN回線で問題が出ないことを確認後に実施（切り戻し保険を1か月確保） |

### B. リモートアクセスVPN（L2TP/IPsec）※config確定版・投入手順書とも作成済（段階2完了 2026-09-15）／実機投入は未 → [[リモートVPN投入手順]]
- VPNユーザー登録（`pp auth username` 業務4名=.17〜.20＝VLAN10到達／開発2名 yohiradev・t.kidodev=.33・.34＝VLAN20到達。2026-09-15復活）
- VPN用フィルタ実装（到達先VLANの TCP 3389/22 のみ許可）
- 同時接続数分のトンネル bind（`pp bind`）
- VPN対象PCのMAC収集 → DHCP予約（`.100`〜`.149` 固定）
- Windowsファイアウォールのリモートデスクトップ許可範囲確認
- Linux端末 sshd/firewalld で `192.168.201.0/24` 許可
- RTX1300 の L2TP/IPsec 同時接続数上限の確認
- L2TP事前共有鍵・VPNパスワードの新規発行
- VPN利用者6名へグローバルIP変更（`124.100.212.73`）を周知

### C. 要検証
- ~~【重要】map-e(固定IP1) でインバウンド UDP500/4500/1701・ESP が通るか~~ → **解決（2026-09-15）**：**固定IP1契約は全ポート利用可能（ポート制限なし）**。ヤマハ公式に「固定IP1（OCNバーチャルコネクト）でL2TP/IPsecリモートアクセス」の設定例あり。→ L2TPは設計どおり成立、方式変更不要。実機での最終接続テストは実装時（段階3）に実施。
- map-e MTU1460 + IPsec のフラグメント挙動（現状疎通OK。大サイズ通信で問題時は `ip tunnel mtu` 調整）

### D. 軽微・その他
- 少数の固定IP機器を `.2`〜`.99` 内へ割当（サーバはAWS移行済のため軽微）
- ネットワーク変更の設定・定義の自動化検討
- 清掃：`config/aws_vpn_yamaha_config.txt`（PSK平文）の削除orマスク
- `config_1200_OCN.txt` の平文認証情報の取り扱い（全般リニューアル予定）

### 後日検討（方針決定済み・未着手）

- **無線LAN認証の強化（VLAN10/20）**：2026-09-14 決定＝当面A（SSIDパスワード強化のみ）、B（MAC ACL）は不採用、**将来C＝RTX1300内蔵RADIUS＋EAP-PEAP へ移行**を検討。**追加機器・費用なし**で実現可（RADIUSはRTX1300内蔵、WBE710はWPA2/WPA3-Enterprise対応）。PEAP（ID/パスワード）から始めるのが現実的。希望時にClaudeが設定手順書を作成。→ 経緯は [[conversations]] 「2026-09-14 無線セキュリティ強化の検討」

### 事後対応

- Web公開環境の新規検討（現行 `192.168.128.128` の公開は移植しない）

### 技術的な未確定事項

**なし。** 設計・configはすべて確定済み。投入後に [[config_RTX1300]] 9-6 の7項目を確認すること。

---

## 10. 切替当日の要点

1. **段階的に投入し、各段階で確認する**（まとめて投入すると切り分け不能）→ [[切替・障害切り分け手順]] 3〜4章
2. **IPv4が通らないときは、まずIPv6取得方式がPDになっていないか**を疑う（罠①）
3. 切り戻しは**LANケーブルをSo-netルータへ差し替え＋PC再起動**のみ（実績のある手順。configの退避は不要）
4. 切り戻し中は**リモートVPNが停止**する。リモート勤務者への影響を判断材料に
