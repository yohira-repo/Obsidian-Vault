- 事業者変更番号が払い出し（【承諾番号】F0814981106　【有効期限】2026/08/14）=> 事業者変更番号をNTT東日本に連絡（8/10 開通）0120-447-423
  8/11 以降に回線プラン変更（光クロス10G) 0120-116-116
- 停止期間は、So-netに接続する。

- [x] **① OCN光コラボの回線、プロバイダー分離**  
- [x] ② NTT東日本に回線変更（1G->10G 9/10予定)  
  - [x] 相談メール送信
  - [x] NTT東日本ID登録
  - [x] 回線変更手続き
  - [x] レンタル機器搬入(9/8)　※貸与機器は **10G-EPON ONU（光加入者終端装置・別体）＋ XG-200KI（HGW）** の2台（XG-200KIはONU一体型ではない）。ONU型番確認済：部品名`10G-EPON〈0〉C ONU〈4〉`／製造番号`2605H018553`／MAC`00:11:57:72:AC:62`（詳細は [[config_XG-200KI]]）
  - [ ] レンタル機器返送（旧HGW/ONU）
  - [x] 総務・営業LANをSo-net回線へ退避（OCNプラン変更日程に合わせ決定）
  - [ ]  回線確認？総務・営業LANを新OCN回線へ戻し 連絡先（0120-594-202(故障)）
- [ ] ③ OCNプラン変更
  - [x] プラン変更申込書作成
  - [x] プラン変更申込（8/25にNTT東日本からアクセスキーが届いた後に申込み　8/27送信済み）
  - [ ] プラン変更確認（9/10+2日程度：決まり次第連絡あり）
- [ ] ④ 社内LAN/WIFI構成変更（含AWS）
  - [ ] HGW（XG-200KI）設定：Wi-Fi OFF（2.4/5/6GHz）、ルータ接続先設定なし（パススルー運用）
  - [ ] 10G-EPON ONU → XG-200KI WAN の CAT6A 結線（10GBASE-T／**CAT6AはONUに付属**）／ONU・XGとも電源はUPS配下（**8個口・不足なし**）
  - [ ] HGW LAN4 → RTX1300 LAN2 の CAT6A 結線（10GbE）
  - [ ] HGW管理画面(192.168.1.1)への保守アクセス手順整備（設定時のみ保守ターミナルを一時直結）
  - [ ] **RTX1300のIPv6取得方式は「RA方式」で設定**（HGW配下のため。PD方式にするとIPv4が通らない）
  - [ ] DNSはパブリックDNSを直接指定（RA方式時のIPv6名前解決不具合の回避）
  - [ ] 社内LANはIPv4のみで設計（VLAN 10・20・30へのIPv6配布なし）
  - [x] IPアドレス設計：VLAN10=192.168.128.0/24 / VLAN20=192.168.64.0/24 / VLAN30(ゲスト)=192.168.192.0/24、GWは各 .1
  - [x] 保守セグメント(RTX1300 LAN1)のセグメント決定（192.168.200.0/24 で確定）
  - [x] 複合機=192.168.128.20（固定）／AKERUNコントローラ=DHCP（固定IP不要）
  - [x] XS508TM=192.168.128.2／WBE710=192.168.128.3 で確定
  - [x] AWS VPCのCIDR確認（10.0.3.0/24。社内セグメントと重複なし）
  - [x] L2TP/IPsecクライアント払出方式の決定（専用セグメント192.168.201.0/24、ユーザー別固定IP、プール未使用）
  - [ ] VPNユーザー登録（pp auth username で業務=.17〜.30／開発=.33〜.46 を個別指定）
  - [ ] VPN用フィルタ実装（到達先VLANのTCP 3389/22のみ許可、他は拒否）
  - [ ] 同時接続ユーザー数分のトンネルをバインド（pp bind tunnel1-tunnelN）
  - [ ] VPNでRDP/SSHする対象PCのMACアドレスを収集し、DHCP予約(dhcp scope bind)で .100〜.149 に固定（全PC分は不要）
  - [ ] Windowsファイアウォールのリモートデスクトップ許可範囲を確認（ローカルサブネット限定になっていないか）
  - [ ] Linux端末のsshd/firewalld設定で192.168.201.0/24を許可
  - [ ] 要確認：RTX1300のL2TP/IPsec同時接続数の上限
  - [x] VLAN10⇔VLAN20間の通信可否を決定（VLAN10→VLAN20のみ許可／VLAN20→VLAN10は拒否・例外なし）
  - [ ] RTX1300に動的フィルタ(ip filter dynamic)でVLAN10→VLAN20の片方向許可を実装
  - [ ] AWS(10.0.3.0/24)への通信はVLAN10のみ許可するようフィルタ・経路を設定
  - [x] VLAN20からの印刷可否（社内は原則紙印刷禁止。VLAN20は紙媒体を直接・間接に利用しないため例外許可なし）
  - [ ] 複合機(192.168.128.20)発のVLAN20宛通信を静的フィルタで拒否（動的フィルタ許可より前段に配置）
  - [x] ネットワーク図.drawio へHGW・IP設計を反映し、ネットワーク図.pngを書き出し（レンダリング不具合3点も修正・再書き出し済）
  - [x] 開通当日の切り分け手順を用意 → [[切替・障害切り分け手順]] を作成
  - [ ] 要確認：固定IP1利用時にHGW側へ必要な設定（接続先設定の扱い・DMZ要否）（OCN／NTT東日本）
  - [ ] 要確認：MAP-E上でAWS VGW向けIKE(UDP500/4500)・ESPが通過するか
  - [ ] VPN利用者6名へ接続先グローバルIPの変更を周知
  - [ ] 外部でグローバルIP(153.156.71.228)を登録している先の洗い出しと変更依頼
  - [ ] 少数の固定IP機器を .2〜.99 内へ割当（サーバ群はAWS移行済みのため軽微）
  - [x] VPNユーザーの業務／開発区分と払出IPを決定（4名/6アカウント）
  - [x] 確認：現行の t-yamashita・takebuchi を登録対象外として問題ないか
  - [ ] L2TP事前共有鍵・VPNパスワードを新規発行（全般リニューアル）
  - [x] 要確認：HGWの192.168.1.0/24と社内VLANサブネットの重複回避（確認済：64/128/192/200 いずれも重複なし）
  - [x] So-net退避／新回線復帰時のHGWを含む結線切替手順の作成 → [[切替・障害切り分け手順]] の「7. 切り戻し手順」
  - [x] RTX1300のファームウェアがRev.23.00.17以降であることを確認
  - [ ] ネットワーク変更の設定・定義の自動化検討
- [ ] ⑤ 【事後対応】AWS拠点間VPNの再構築（代替ルートがあるため回線切替後に実施）
  - [ ] AWS Customer Gatewayを新グローバルIPで再作成
  - [ ] VPN接続を張り直し、設定ファイル(ベンダー=Yamaha)を取得
  - [ ] RTX1300へ投入し BGP/経路を確認（自AS 65000／対向AS 10124／広報 192.168.128.0/24）
- [ ] 【事後対応】Web公開環境の新規検討（現行の192.168.128.128公開は移植しない）
- [ ] ⑥ So-netコラボ解約
- [x] その他、機器購入（ルーター、スイッチ、無線AP)
