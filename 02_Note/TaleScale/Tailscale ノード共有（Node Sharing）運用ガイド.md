
個人所有PC（自宅）から会社所有のリモートアクセス用PC（会社資産）へ、**無料（FREEプラン）かつ安全にリモート接続環境を構築・運用するための手順書**です。

---

## 1. 概要・構成図

### システム構成イメージ
```
[会社側]
  └── リモートアクセス対象PC（会社資産）
        │ (Tailscale：会社用アカウント等でログイン)
        │
        ├── Share（ノード共有）機能でアクセス権を個別付与
        │
[自宅側（10名）]
  ├── メンバーA の自宅PC (Tailscale：個人Googleアカウント等)
  ├── メンバーB の自宅PC (Tailscale：個人Googleアカウント等)
  └── ... メンバーJ の自宅PC (Tailscale：個人Googleアカウント等)
```

### この構成のメリット
- **完全無料（0円）で運用可能**：組織アカウントの有料化（10名分）を回避できます。
- **高いセキュリティ**：アカウント使い回しを行わず、各自の個人アカウントを使用するためアクセス元の特定が可能です。
- **最小限の権限**：自宅PCからは「共有されたリモートPC」にのみアクセス可能です。
- **即時アクセス遮断**：退職や変更時は、管理画面から該当ユーザーの共有（Share）を解除するだけで一瞬でアクセス拒否できます。

---

## 2. 準備・事前作業（ホストPC：会社側資産の設定）

リモート接続される側のPC（会社資産）で事前に以下の設定を行います。

### ① 常時起動・自動接続設定（Windowsの場合）
1. Tailscale をインストール後、タスクバー（通知領域）の Tailscale アイコンを右クリックします。
2. **`Run as Unattended Service`（無人サービスとして実行）** にチェックを入れます。
   - *理由: PC起動時、ユーザーのログイン前（Windowsサインイン画面）でも自動でTailscaleを接続状態にするため。*

### ② キー有効期限の無効化（Key Expiry の解除）
1. [Tailscale Admin Console](https://login.tailscale.com/admin) にログインします。
2. 左メニューの **[Machines]** タブを開きます。
3. 該当のPC（ホストPC）を選択し、メニューから **[Disable key expiry]** を設定します。
   - *理由: デフォルトの180日（6ヶ月）ごとの再認証・接続切れを防止するため。*

---

## 3. ノード共有（Share）の設定手順

会社PCを10名のメンバーに共有する際の手順です。

### 【ステップ1】共有リンクの発行（管理者）
1. Admin Console の **[Machines]** タブを開きます。
2. 共有したいリモートPCの右端にある **「...」** メニューをクリックします。
3. **[Share...]**（共有）を選択します。
4. **「Generate share link」** をクリックし、発行された共有URLをコピーします。

### 【ステップ2】メンバーへの接続案内（各ユーザー）
1. 接続したいメンバー（10名）に共有URLを送付します。
2. メンバー各自は、自分のPCで Tailscale をインストールし、**個人用の無料アカウント（Google/Microsoft等）** でログインします。
3. ログイン後、送られてきた共有URLをブラウザで開き、**[Accept]（承認）** をクリックします。
4. これで、各自の Tailscale ネットワーク上に会社のリモートPCが自動的に追加されます。

---

## 4. 日常の運用ルール・注意事項

| 項目 | 内容・注意点 |
| :--- | :--- |
| **PCの電源仕様** | 毎日 19:00 シャットダウン / 8:30 自動起動の運用でも、起動と同時に Tailscale が自動接続されます。 |
| **リモートデスクトップ接続** | 接続時は、Tailscale の `100.x.y.z`（100から始まるIPアドレス）を指定してリモートデスクトップ（RDP）やSSH接続を行います。 |
| **メンバー追加・変更** | 共有リンクを新しいメンバーに共有・承認してもらうだけで即座に追加可能です。 |
| **退職・アクセス停止対応** | [Machines] -> [Share] の共有メンバー一覧から、該当ユーザーの **[Remove]** をクリックするだけでアクセスを安全に剥奪できます（パスワード変更等は不要）。 |

---
*作成日: 2026年8月3日*
