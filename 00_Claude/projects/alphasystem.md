# alphasystem 作業ログ

## 2026-07-27 MFクラウド連動タスクの棚卸し・優先順位付け

- **経緯**: `alpha-mfc-join/README.doc`(MFクラウド連動ブレーンストーミング資料)を読み、今後のタスクについて相談。
- **結論**:
  - 契約管理・プロジェクト管理のMF連携は資料通り保留とし、今回のタスク棚卸しの対象外とする。
  - 優先順位の基準は「データの上流工程(依存関係)」。現状のalphasys⇔MFクラウド間の入り繰り・手動CSV連携を解消する方向を重視する。
  - タスクを以下の順で分解した(詳細は [design/2026-07-27-mfc-cloud-integration-roadmap.md](design/2026-07-27-mfc-cloud-integration-roadmap.md)):
    0. MFクラウドAPI連携基盤(新規追加。資料には無いが全タスクの前提)
    1. 顧客管理連動(取引先マスタ同期)
    2. 請求管理(売上)
    3. 請求管理(仕入)
    4. 給与連携
  - 調査の結果、alphadb/alphasysには顧客・契約・請求・原価・部門の社内側モデルは既に整備済みだが、MFクラウドとのAPI連携(認証・クライアント・外部IDマッピング)はどのリポジトリにも未着手であることを確認した。
- **次のアクション**: タスク0(MFクラウドAPI連携基盤)から詳細設計(spec)に着手する。
- **補足(後日改訂)**: 経費管理の追加とAPI提供状況の判明により、タスク番号は 0=基盤 / 1=顧客 / 2=売上請求 / 3=仕入請求 / 4=経費管理 / 5=給与 に改訂された。下記2026-07-27の3件目の記録を参照。

## 2026-07-27 MFクラウド各製品の公開API提供状況(Web調査)

- **経緯**: 上記ロードマップのタスク0〜4の前提として、連携対象4製品の公開API提供状況を公式情報で調査。
- **結論(製品別)**:

  | 製品 | 公開API | 認証方式 | 主な条件 |
  | ---- | ------- | -------- | -------- |
  | クラウド会計 | あり(マネーフォワード クラウド会計API) | OAuth 2.0 のみ(APIキー非対応) | 有償プラン利用者は追加費用なし。アプリポータル登録が必要(全権管理者権限) |
  | クラウド会計Plus | あり(ドキュメント非公開) | 未確認 | 担当者への問い合わせが必須。開発者サイトには未掲載 |
  | クラウド請求書 | あり(クラウド請求書API v3.1.0) | OAuth 2.0(scope: `mfc/invoice/data.read` / `.write`) | 追加費用なし。無料/スモール等の下位プランは書類種別ごと100リクエスト上限、ビジネス以上で無制限 |
  | クラウド債務支払 | あり(クラウド債務支払API) | OAuth 2.0 Authorization Code Grant | 債務支払の利用者。製品ごとに個別アプリ登録が必要 |
  | クラウド給与 | **一般公開なし**(API v2 は連携パートナー限定) | identifier + office_api_key + 接続元IP制限 | ドキュメントに「※一般ユーザーへの解放はしていません」と明記 |

- **重要な判明事項**:
  - 開発者サイト([developers.biz.moneyforward.com](https://developers.biz.moneyforward.com/))に掲載されているのは **クラウド会計 / 経費 / 請求書 / 債務支払 / クラウドインボイス[受領]** の5製品。**会計Plusと給与は未掲載**。
  - タスク1(取引先マスタ同期)・タスク2(請求・売上)・タスク3(請求・仕入)は公開APIで実現可能。
  - **タスク4(給与連携)は公開APIが存在せず、CSV連携が現実的な代替手段**。ロードマップの見直しが必要。
  - 会計Plusを使う場合はMF担当者経由でのドキュメント入手が前提となり、リードタイムを見込む必要がある。
- **次のアクション**: タスク0の詳細設計では OAuth 2.0(Authorization Code Grant)+ アプリポータル登録を前提とする。給与連携はCSV前提に方針変更するか、MF社に一般公開予定を確認する。

## 2026-07-27 タスク0(MFクラウドAPI連携基盤)の設計確定とロードマップ改訂

- **経緯**: API提供状況の調査結果を受けて方針を確定し、タスク0の詳細設計を行った。あわせて元資料に「経費管理」が追記されたため、ロードマップに反映した。
- **確定した前提**:
  - 会計は**クラウド会計(通常版)**を利用中。会計Plusではないため、公開APIで自己申請ベースで着手できる。
  - 仕入側(タスク3)の連携先は**クラウド債務支払 API**とする(クラウドインボイス[受領] APIではない)。
  - 給与連携は公開APIがないため、**CSV連携の自動化**で進める。
  - シークレット管理は **AWS Secrets Manager(Client ID/Secret・暗号化キー)+ alphadb(access/refresh token を暗号化保存)** の組み合わせとする。alphaexapi は現状 Secrets Manager 未使用のため新規導入となる。
- **ロードマップ改訂**: 元資料への「経費管理」追記を受け、タスクを 0=基盤 / 1=顧客連動 / 2=売上請求 / 3=仕入請求 / **4=経費管理(新規)** / 5=給与 に改訂。経費は公開APIがありOAuth基盤に乗るため、CSV方式となる給与より先に着手する。
  - これにより「OAuth基盤(タスク0)が会計・請求書・債務支払・経費の4製品を賄い、給与のみ別方式」という切り分けが明確になり、基盤に投資する価値が高まった。
- **タスク0の設計方針**(詳細は [design/2026-07-27-task0-mf-api-foundation-spec.md](design/2026-07-27-task0-mf-api-foundation-spec.md)):
  - **連携コードの置き場所は alphaexapi に集約**。理由: OAuth Authorization Code Grant はHTTPSのリダイレクトURIが必須でHTTPサーバを持つ alphaexapi しか受けられない。またトークンのリフレッシュを1箇所に集約しないと同時更新で refresh token が競合する。既存の google-drive の「provider + 用途別モジュール」二層構成に合わせる。
  - **MF側の外部IDは汎用マッピングテーブル`MfLink`1本で保持**。理由: 製品ごとに個別アプリ登録が必要なため顧客1件が製品ごとに別IDを持ち、業務テーブルにIDカラムを持たせるとエンティティ×製品でカラムが増殖する。1テーブルに寄せると既存スキーマを変更せずに済み、`syncStatus`/`syncedAt`で現状の入り繰りを追跡できる。
  - トークンは `MfOauthToken` テーブルに製品単位(`unique(mfProduct)`)で保持。ECS複数タスクでの並行リフレッシュは行ロックで直列化する。
  - 請求書APIの1秒3回制限に対応するため、共通クライアントに製品別スロットルと429の指数バックオフリトライを持たせる。
- **次のアクション**: タスク0の実装計画(plan)を作成する。未確定事項として、MFサンドボックス環境の有無、HTTPS必須のリダイレクトURIのローカル開発時の扱い、ECSタスク定義へのシークレット注入経路の確認が残る。

## 2026-07-27 MFクラウド経費APIの調査結果とタスク0設計の修正

- **経緯**: タスク4(経費管理)で「社員・プロジェクト別の原価データ」が実際にAPIで取得できるかを、公開OpenAPI仕様(Swagger)の実物で検証した。
- **タスク4の実現可能性: 確認済み**。経費明細(`ExTransaction`)に `project_id` / `dept_id` / `office_member_id` / 金額 / 計上日 が揃っており、`project_id`・`office_member_id` でサーバー側フィルタも可能。プロジェクト・従業員・部門の各マスタもAPI取得できる。
- **タスク0の設計を修正した4点**:
  1. **製品ごとにOAuth・APIのエンドポイントとスコープが異なる**ことが判明(経費は `expense.moneyforward.com/oauth/authorize`、スコープは `office_setting:write` 等の独自体系。請求書は `mfc/invoice/data.read`)。→ 製品別設定 `money-forward-product.config.ts` として抽象化し、業務モジュールから差異を隠蔽する構成に変更。
  2. **レート制限の具体的な数値は公式非公開**で「エンドポイントごとに独立、状況により変化」とのみ記載。経費APIのSwaggerには429の記述すらない。→ 固定スロットルのハードコード前提を撤回し、**429ベースの適応的バックオフを主軸**、判明済みの値(請求書の1秒3回)のみ設定として併用する方針に変更。
  3. **ページング方式も製品ごとに異なる**(経費は `page` + `next`/`prev` パス方式で `per_page` が使えず、共通仕様の `cursor`/`limit` と不一致)。→ 共通クライアントがページングを吸収し、業務モジュールには統一インターフェースを提供する責務を追加。
  4. **経費はread専用スコープが存在せず** `office_setting:write` が必須。→ 最小権限が取れないことを受け入れ、トークン保護(暗号化保存・Secrets Manager)を徹底する方針を明記。
- **その他の重要な判明事項**:
  - **データ連携用のWebhookは存在しない**。同期はポーリング型バッチ前提。
  - **経費明細に `updated_at` での絞り込みがない**(レスポンスには含まれるがクエリ不可)。→ 計上日ベースで直近数ヶ月をローリング再取得し `updated_at` 突き合わせで upsert する設計になる。
  - トークンは承認したユーザーの権限で発行されるため、**事業者全体のデータ参照には管理者権限ユーザーでの認可が必要**。バッチ用に管理者アカウントを1つ用意する運用になる。
  - トークン有効期限は製品ごとに大きく異なる(経費は約32日)。固定値を仮定せず `expires_in` から算出する。
  - **運用上の前提**: APIは入力された値を返すだけなので、MF経費側でプロジェクト機能を有効化し**経費登録時のプロジェクト選択を必須にする運用**がないと、原価がプロジェクトに寄らない。実装前に運用ルールの整備が必要。
- **検討候補として記録**: **マネーフォワード クラウド個別原価**が2025年11月にAPI提供を開始している(プロジェクト別工数管理・個別原価計算、労務費計算と間接費配賦)。要件が労務費・工数まで含む場合はこちらが適合し、給与連携(タスク5)の代替にもなり得る。ただし仕様は非公開でMF担当者への問い合わせが必要。MF経費のプロジェクトマスタは個別原価と同期する旨が公式記載されているため、「経費(実費)+ 個別原価(労務費・工数)」の組み合わせが本来のプロジェクト別原価の姿になり得る。
- **次のアクション**: タスク0の実装計画(plan)作成。あわせて、クラウド個別原価APIをMF社に問い合わせるかを判断する。

## 2026-07-27 タスク0の実装計画作成

- **経緯**: spec のマージを受けて、タスク0の実装計画を作成した。詳細は [design/2026-07-27-task0-mf-api-foundation-plan.md](design/2026-07-27-task0-mf-api-foundation-plan.md)。
- **計画の範囲**: **alphadb(スキーマ)+ alphaexapi(money-forwardモジュール)まで**とし、AWS Secrets Manager 導入とECSへのシークレット注入は対象外とした。理由: alphaexapi のECSタスク定義へのシークレット注入経路が未確定で、含めると着手前に調査待ちになる。この範囲ならMF実APIもAWS変更も不要で、モックによる単体テストで完結する。
- **実装は別リポジトリのため、alphadb と alphaexapi でそれぞれ別ブランチ・別PRになる**(12タスク構成)。
- **既存コードベース調査で判明した制約と、それに対する設計判断**:
  - **alphaexapi は現在ローカルでビルドできない**。`alphadb` が AWS CodeArtifact 配布の private パッケージで未インストールのため、`npm run build` が9件のエラーで失敗する。
    - → 対策として `MfPrismaPort` という最小インターフェースを alphaexapi 側で宣言し、モジュール配線時に `useExisting: PrismaService` で実装を注入する設計にした。これによりリポジトリの単体テストがレジストリ認証なしで完結する。依存の向きを逆転させる設計上の利点もある。
  - **テストが実質存在しない**(`app.controller.spec.ts` の1ファイルのみ)。テストパターンの確立も計画に含めた。
  - **複合ユニーク制約(`@@unique`)の使用例が既存スキーマにない**。`MfLink` で初めて導入する。「1エンティティ・1製品につきMF側IDは1つ」をDBで保証するために必要と判断。
  - **スケジューラが未実装**(alphabatch はSQS投入→worker実行方式)。定期同期が必要になるタスクで別途用意する。
- **設計上の重要な判断**:
  - **OAuth `state` はHMAC署名で完結させ、DBに保存しない**。ECSが複数タスクで動いてもどのタスクがコールバックを受けても検証でき、テーブルを増やさずに済む。有効期限はMF側の認可コード有効期限に合わせ10分。
  - **リフレッシュの直列化はPostgreSQLのアドバイザリロック(`pg_advisory_xact_lock`)を使う**。行ロックではなく名前付きロックにすることで、行が存在しない場合も直列化できる。
  - **`/callback` には既存の `GoogleAuthGuard` を適用できない**。MFからのブラウザリダイレクトで呼ばれるため Google IDトークンや `x-user-role` ヘッダーを持たない。`/authorize` にのみガードを付け、`/callback` は `state` の署名検証で保護する。
  - **レート制限の事前スロットルはプロセス内の情報しか持たない**ため、ECS複数タスクでは全体のレートを厳密に保証しない。429を減らす緩和策と位置づけ、適応バックオフを主軸とする。
- **完了後に別途必要な残作業**: alphadb の CodeArtifact への publish(外部影響のある操作なので実施前に確認)、MFクラウドの製品ごとのアプリ登録(全権管理者権限が必要。この時点で会計・請求書・債務支払の実際のURL・スコープが判明する)、Secrets Manager 導入とECS注入、実環境での疎通確認、alphasys側の管理画面導線。

## 2026-07-27 alphaaimail(営業メールダッシュボード)の社内システム統合検討

- **経緯**: デモ版として稼働中の alphaaimail(Gmail 営業メールを Claude API で分類し受発注状況をダッシュボード化)を社内システムに統合したい。テーブル単位で関連付けられるものの洗い出しから検討を開始。alphaaimail / alphasystem / alphadb の3リポジトリを調査した。
- **テーブル突合の見立て**(詳細は [design/2026-07-27-aimail-integration-study.md](design/2026-07-27-aimail-integration-study.md)):
  - deals の `direction`(receiving/ordering)は Contract の `orderContract`(NULL=売上/非NULL=発注)と1対1で対応し、最も自然な結合軸。
  - deals の `doc_number` は接頭辞で発番元が判別でき(PO=NTTデータ系、CFZ1UZ=Fieldglass、AC=自社案件コードと推定)、Contract/BillRecipt への写像に使える。ただし重複があり事前解消が必要。
  - 社内送信者は `alp_users.email`(UNIQUE)で確実に突合可能。
- **判明したギャップ(統合の前提課題)**:
  1. alphaaimail は From/To/Cc アドレスを DB に保存していない(Gmail API から取得はしているが保存時に欠落)。メールアドレス突合が現状不可能で、これが最初の改修になる(gmail_id が残っているため遡及取得は可能)。
  2. 社内DBに「先方担当者」テーブルが存在しない(`alp_customer.email` は1社1件のみ)。`alp_customer_contact` の新設と、MfLink と同じ思想の汎用マッピングテーブル `alp_mail_link` の追加を提案。
  3. alphaaimail の取引先名寄せは品質が低く(companies 59件中 NTTデータ系だけで12レコード重複)、customerNo 突合には名寄せ層の再設計が必要。
  4. DB基盤の相違(SQLite vs PostgreSQL/alphadb)、金額充足率17%(33/196件)。
- **結論(推奨方針)**: 段階統合とする。フェーズ0=セキュリティ手当+アドレス保存改修 → フェーズ1=alphadb にリンク用2テーブル追加、突合バッチ+紐付け確認UIで突合精度を実データ検証 → フェーズ2=PostgreSQL 移行・パイプラインの alphaexapi 移設・UI の alphasys 統合。フェーズ2は MF タスク0(OAuth 基盤・Secrets Manager)の成果物を流用できるため、その完了後の着手が効率的。
- **統合以前に対処が必要な件**:
  - alphaaimail/README.md 57行目に Anthropic API キーが平文でコミット済み(GitHub プッシュ済みのため即ローテーション要)。alphaexapi の credentials.json、alphasys/README の OAuth Secret にも同様の問題あり。
  - alphaaimail の REST API は無認証(物理削除エンドポイントやパイプライン起動まで公開)。
  - `.gitignore` に `data/`・`config/` が未追加で、実メールデータ入り SQLite がコミットされ得る。
- **次のアクション**: 未決事項の確認から着手する — AC コードの発番元(Project か Contract か)、alphadb の本番データ移行状況(alp_users は D1 未決)、doc_type 14分類の社内側での扱い、要員営業系(staff_request 等)のスコープ判断、フェーズ2の実施時期。

## 2026-07-27 alphaaimail統合の基本方針確定(契約締結を境界としたデータ所有権)

- **経緯**: 上記検討資料の「3.1 対応表」について方針の指示があり、3章を改訂した。
- **確定した基本方針**:
  1. 契約締結以前(案件レベル)は alphaaimail 側のテーブルを使用する
  2. 契約締結時に、alphaaimail 側のテーブルから社内システムへコピーする
  3. 契約締結以降は社内システムに寄せる
- **この方針の効果(重要)**: 同じ案件を2システムで並行管理する期間が発生しないため、統合の課題が「**2つのDBを継続的に突き合わせる**」問題から「**締結時に一度だけ写す**」問題に変わった。結果として、当初「統合の前提課題」としていた以下は重要度が下がった。
  - **名寄せ品質**(companies 59件の表記ゆれ): 全件の自動突合が不要になり、締結する案件のみ人手確認すればよい
  - **金額の充足率17%**: 正式金額は締結時に社内システムで入力するため、金額による自動照合を行わない
  - **doc_number の重複**: 締結時に人が対象を選ぶため自動処理を壊さない。事前の一括重複解消が不要
  - → 逆に重要度が高いまま残るのは「**From/To/Cc アドレスの保存改修**」と「**先方担当者テーブルの新設**」の2点。締結時の担当者登録と締結後メールの紐付けに必須。
- **フェーズ1の位置づけが変わった**: 「全件を自動突合するバッチ」ではなく「**締結時に一度写すコピー機能**」になる。人手確認が締結という業務判断に自然に組み込まれるため、自動突合の前提条件(名寄せ精度・金額精度・番号重複解消)を満たす必要がなくなり、難度と範囲が下がった。
- **設計上の追加事項**:
  - `alp_mail_link` に `copyStatus` / `copiedAt` を追加。同一 deal の二重契約化を防ぐ冪等性の担保と、締結前後の判別に使う。
  - 締結後も請求書送付・入金通知などのメールは届き続けるため、**リンクテーブルは締結後も必要**(所有権が社内に移っても紐付けは残る)。
- **想定として置いた事項(要確認)**: ①締結の契機は社内システムでの Contract 登録操作とする(doc_type による自動判定は誤分類リスクがあるため採らない) ②締結後の deal の扱い ③コピーは一方向で、社内側の変更を alphaaimail へ戻さない(戻すと双方向同期になり方針の利点が失われる)。いずれも7章の未決事項に記載した。

## 2026-07-27 訂正: ダッシュボードは締結の前後をまたいで表示する

- **経緯**: 上記の想定②「締結後の deal はクローズしてダッシュボードの要対応から外す」は**誤り**との指摘を受けて訂正した。
- **正しい前提**: **締結後も deal はクローズしない。** 締結後は契約の更新・請求などのメールが届くため、その動きを追う必要がある。ダッシュボードで見えるのは次の2つ。
  1. **営業中の案件の動き**(締結前)
  2. **現在契約している案件の動き**(締結後)
- **この訂正から導かれた設計上の帰結**:
  - 「**所有権(どちらのDBが正か)**」と「**ダッシュボードの表示範囲**」は別の軸として書き分ける必要がある。所有権は締結時に社内システムへ移るが、表示は前後をまたいで continue する。
  - 締結後の区分を表示するには、**メールの動き(alphaaimail)と契約情報(社内システム)の両方を突き合わせる**ことになる。したがって `alp_mail_link` は監査用の記録ではなく**ダッシュボード表示の結合キーそのもの**になった。
  - **DB一本化(案B / 案C フェーズ2)の価値が上がった**。2系統のままだとダッシュボードがSQLite⇔PostgreSQLをまたぐ結合をアプリ側で行うことになり、恒久的なコストになる。案Aの欠点として明記した。
  - **フェーズ分割に注意点が生じた**: フェーズ1時点ではDBが2系統のため、「契約中の動き」の表示は (a)捨てることになる暫定的なDBまたぎ参照を作る か (b)フェーズ2まで待つ かの選択になる。**(b)を推奨**とした(捨てる実装を避けられ、フェーズ1でも「案件から契約への引き継ぎ自動化」という独立した価値が出る)。ただしダッシュボードの完成形がフェーズ2依存になるため、**フェーズ2は前提として計画すべき**と位置づけを変更した。
- **新たな未決事項**: 締結後のダッシュボードの表示単位を、元の deal のまま(契約情報を Contract から引く)とするか、Contract を単位に切り替える(deal はリンクとして保持)とするか。

## 2026-07-27 deal と Contract は1対N(SESの実態)— 表示単位を Contract に確定

- **経緯**: 上記の未決事項について、「**SESでは締結前に1案件として活動していたものが、契約時に複数案件(要員ごとの契約など)に分離されることがよくある**」との情報を受けて確定した。
- **確定事項**: **deal と Contract は 1対N**。ダッシュボードの行の単位は締結を境に **deal → Contract** へ切り替える。
- **判断根拠(両案のデメリット比較)**:
  - **deal を行の単位のまま維持する案のデメリット(採用せず)**: 1 deal に3契約が入ると1行に3契約が丸められる。契約ごとに金額・期間・請求状況が異なるのに集計が意味を持たなくなり、「**どの**契約の請求書が未送付か」「**どの**契約の更新期限が近いか」を行として出せない。要員ごとに契約が分かれるSESでは実務要件そのものを損なう。
  - **Contract を単位に切り替える案のデメリット(採用)**: 実質的なコストは「**締結後に届いたメールをどの契約に紐付けるか判定が必要**」の1点に集約される。deal 単位ならこの判定は不要だった。副次的に、営業中の1行が契約中の複数行に増えてUI上の連続感が途切れる(Contract → deal の逆リンクで商談経緯を辿れるようにして緩和)、一覧クエリが2系統になる。
  - → 前者は要件を損なうが後者は解ける実装課題に収まるため、後者を採用。
- **1対N の確定により訂正した設計2点(重要)**:
  1. **3.1.2 の「deal と Contract を1対1に固定する」は誤りだったため訂正**。1対N に修正し、締結後メールの契約特定という新たな課題を明記した。判定材料は doc_number(最も確実)、要員名(ContDetail.memberId と突合。SESの典型ケース)、金額(補助のみ)。**特定できない場合は deal 単位でリンクし(`entityType='DEAL'`)、人手で振り分けられるようにする**(取りこぼさないことを優先)。
  2. **4.1 の「COPIED が既にあれば契約化済みと判定して二重契約化をブロックする」ロジックは成立しないため撤回**。1 deal から複数契約を起こすのが正常なため、既存レコードの存在をエラー判定してはならない。冪等性は「同じコピー操作の二重実行を防ぐ」粒度で担保し、UIでは「その deal から既に起こした契約の一覧」を表示して人が判断する方式に変更した。なお `@@unique([entityType, localId, mailKey])` は同一 deal から異なる契約への複数リンクを許すため、この方針と整合する。
  - また 3.1.1 のコピー操作の定義を「deal を1件の契約にする」から「**deal を元に契約を1件起こす操作を必要な回数繰り返す**」に改めた。
- **次のアクション**: 締結後メールの契約特定方式の検証(実データで doc_number / 要員名のどちらがどの程度使えるか)。

## 2026-07-27 SQLite破棄・取引先は案件発生時点から社内システム参照・担当者は別テーブル

3.3(統合の前提課題)への指摘を受けて4点を確定した。

- **① メールアドレスをキーにした検索は行う(担当者は別テーブルとする)**
  - 受信メールの処理が「From アドレス → 取引先・担当者を特定 → 関連する案件・契約を絞り込む」という流れのため、アドレス検索は統合の中核機構。
  - **JSON列で複数保持する案は採らない**。理由は①メールや案件を担当者に紐付けるための安定したIDが必要(JSON配列要素にIDを持たせるのは実質テーブルの再実装) ②属性が増える(氏名・電話・有効フラグ・退任日) ③一意性制約が効かない(`@@unique([customerNo, email])` が使えない)。なお PostgreSQL なら JSONB + GIN で検索自体は可能なので、「検索できないから」ではない。
  - **「1人の担当者が弊社の多案件を担当」(相手企業の規模が小さい場合に多い)は問題にならない**。アドレスで特定できるのは「どの取引先の、誰から」までで、「どの契約か」は doc_number / 要員名で決めるという役割分担のため。ただし**アドレスを契約特定のキーに使ってはいけない**という注意点として明記した。同一アドレスが複数取引先に登録される場合(転籍・仲介)は候補提示して人が選ぶ。
- **② SQLite版はデモとして破棄。データ移行・DB移行は不要**
  - これにより **5章の統合方式が組み替わった**。「疎結合連携(SQLite維持)」と「段階移行(後からPostgreSQLへ移す)」はいずれも不成立で、**最初から社内システム基盤上に作り直す一択**になった。
  - 前回懸念していた課題が消滅: DBが2系統になることによる SQLite⇔PostgreSQL の結合、「契約中の動き」表示のために後で捨てる暫定実装、取引先マスタの二重管理。
  - **From/To/Cc の遡及取得(Gmail再取得)も不要**になった。データを破棄するため、新規実装で最初から正しく保存すればよい。
  - 実装順序を「移行のためのフェーズ分け」から「動くものを早く出すための順序付け」に変更(前提整備 → パイプライン → ダッシュボード(営業中) → 締結時コピー → ダッシュボード(契約中))。
  - なおデモ版のセキュリティ課題(README のAPIキー平文、無認証REST API、.gitignore 未整備)は**破棄しても解消しない**(既に公開・コミット済み)ため、破棄までの対処が必要と明記した。
- **③ 取引先は案件発生の時点で社内システムを参照する**(新規案件でも半数以上は既登録の取引先であるため)
  - **所有権モデルを修正**: 段階で所有権が移るのは案件・契約(取引の実体)のみで、**取引先マスタは常に社内システムが正**とした(3.1.0d を新設)。メール側に取引先マスタ(companies)を独自に持たない。
  - これに伴い、前回「重要度: 低」に下げた**名寄せを「重要度: 中」に戻した**。締結時まで先送りできず案件発生時に突合が必要なため。ただし全件の事前一括名寄せは不要で、案件ごとに1件ずつ突合すればよい。
  - 3.1.1 のコピー内容から取引先の行を削除(締結時点で既に紐付いているためコピー対象外)。
- **次のアクション**: 締結後メールの契約特定方式の検証。特に「要員ごとに契約が分かれるとき、注文書番号も要員ごとに別番号が振られるのか、案件で1番号なのか」の確認。あわせて、破棄前にデモ版データを発番規則の分析用途で参照するかの判断。

## 2026-07-28 project の定義確定 — メール由来データとは無関係

- **経緯**: 検討資料で `deals.project_name`(AIがメールから抽出した案件名)を `Project` にコピーする設計にしていたが、project の定義が示され**誤りだったため訂正した**。
- **確定した project(alp_project)の定義**:
  - **社内独自のもの。対外的なメール内容には一切関連しない**
  - **契約(Contract)をグルーピングしたもの**。①同じ時期に同じ業務だが客先要請で契約を複数に分けているケース ②同じ業務の契約延長・変更だが客先要請で新規契約として起こすケース をまとめる
  - **契約締結時に、管理担当者が判断する**
- **ここから確定した3点**:
  1. **メールから Project を導出してはならない**。`deals.project_name` は Project とは別概念。名前が似ているため取り違えやすい点を文書に明記した
  2. **コピー処理で Project を自動作成しない**。締結時のコピーが作るのは Contract のみで、どの Project に属させるかは管理担当者の判断(自動化の対象外)
  3. **Project は deal のグルーピングではなく Contract のグルーピング**。1 deal → N 契約であり、かつ客先要請による新規契約は別の deal から発生しうるため、**1つの Project に複数の deal 由来の契約が含まれ得る**。deal と Project の間に安定した対応関係はない
- **訂正した箇所(6箇所)**: 3.1.0a 所有権表(「必要なら Project も作成」→ 管理担当者が判断)、3.1.1 コピー内容(project_name の行をコピー対象外に)、3.2 発番体系表(AC コードの対応先から Project を除外)、4.1 MailLink の `entityType` から `'PROJECT'` を削除(Project は Contract 経由で辿る)、7章の未決事項、および 3.1.0e を新設。
- **副次的に解決した未決事項**: 「`AC806` 等の自社案件コードは Project と Contract のどちらの発番か」について、**Project は対外メールに現れないため Project ではないことが確定**した。Contract の契約番号か、それ以外の社内発番かの確認のみ残る。
- **表示面での活用として記録**: Project はメール連携の対象外だが、**ダッシュボードのグルーピング軸として有用**。客先要請で1業務が複数契約に分割された場合、契約単位の行では業務の実態が見えにくいため、Project 単位でまとめて表示すれば分割された契約群を1つの業務として見られる。行の単位は Contract のままとし、Project は折りたたみ/集計の軸として使う。

## 2026-07-28 補足: Project の紐付けは deal 時点でも可能(締結時に限らない)

- **経緯**: 上記で「Project の割り当ては締結時に管理担当者が判断」と書いたが、**deal の時点でも特定できる場合がある**との補足を受けて修正した。あわせてダッシュボードに Project を出す方針が確定した。
- **正しい前提**: Project の決定は**常に人の判断**であるという点は変わらないが、その判断のタイミングは締結時に限らない。案件の性質で2つに分かれる。

  | deal の性質 | Project の特定 | 扱い |
  | ---- | ---- | ---- |
  | **既存契約に関連する案件**(要員増員、契約延長など) | deal の時点で特定できる | **既存 Project への紐付けを手動で行う**。締結を待たない |
  | **完全な新規案件** | deal の時点では特定できない | 締結時に管理担当者が判断。**この deal がそのまま1つの新規 Project になる可能性がある**(deal = project) |

- **データモデルへの反映**: **deal に nullable な Project 参照列を持たせる**(未特定なら空)。SQLite版を破棄して deal を alphadb 上に置くため、リンクテーブルを介さず列で表現できる。`alp_mail_link` の `entityType` に `'PROJECT'` を設けない理由も、この設計に合わせて「メールから Project へ直接リンクを張らず、deal の属性として持つため」に整理し直した。
- **ダッシュボードに Project を出す方針が確定**。営業中・契約中の双方でグルーピング軸として使う。
  - **契約中**: 客先要請で1業務が複数契約に分割された場合、Project 単位でまとめると業務としての実態が見える
  - **営業中**: 既存契約関連の案件は deal 時点で Project が特定できるため、**既存 Project の下に進行中の引合を並べられる**(「この案件で増員の話が2件動いている」が一目で分かる)
  - 行の単位は営業中=deal / 契約中=Contract のままとし、Project は折りたたみ・集計の軸とする。完全な新規案件は締結まで「Project 未割り当て」区分に置く。
- **確定した2点(2026-07-28 追記)**:
  1. **Project 名は deal の情報を初期値として提示してよい**(入力の手間を減らすため)。ただし決定は人が行う原則は維持する。初期値の提示は自動導出ではなく入力補助であり、管理担当者がそのまま採用するか書き換えるかを選べる形にする。
  2. **Project の紐付けは管理担当者が実施する**(営業担当は行わない)。したがって操作を権限で制限し、導線も管理担当者向けに用意する。deal の発生から Project 割り当てまでに時間差が生じるため、その間 deal は「Project 未割り当て」状態で正常に扱えなければならない。
  - 導線については、**「Project 未割り当て」区分をそのまま管理担当者の作業キューとして兼用する**方針とした。既存契約関連の案件がここに溜まれば紐付け漏れが分かるため、専用導線を別に作る必要がない。
- **新たに判明した課題**: **`alphadb` の `Role` enum は `USER` / `ADMIN` の2値のみで、「管理担当者」に相当する区分が存在しない**(Users.role のみ。RoleType は MEMBER/SUB_LEADER/LEADER で要員の役割)。`ADMIN` で代替するか区分を追加するかは要判断で、追加する場合は alphadb のスキーマ変更となり影響範囲が広い。7章の未決事項に記載した。

## 2026-07-28 コミット済み機密の調査と、シークレット一括管理方針の検討(継続中)

### コミット済み機密の実態(当初の報告は不正確だった)

当初「alphaaimail/README.md 57行目の Anthropic API キー」と報告したが、**調査したところより広範だった**。README は既に修正済みで、実際の問題は次のとおり。

- **`.env` と `config/credentials.json` そのものが Git 追跡下にあり push 済み**(3リポジトリ)
  - alphaaimail: `.env` / `config/credentials.json` — **2026-04-30 の初回コミットから**
  - alphasys: `.env`
  - alphaexapi: `.env` / `.env.production` / `config/credentials.json`
- **見逃された理由**: alphaaimail の `.gitignore` には `.env`(21行)と `config/credentials.json`(39行)が記載されている。しかし**先にコミットされたファイルには `.gitignore` が効かない**。alphasys / alphaexapi は `.env*.local` のみで `.env` は除外対象外だった
- いずれも private リポジトリだが、履歴に残るため無効化が必要
- **対処済み(2026-07-28)**: alphaexapi の AWS 資格情報(無効化・削除)、alphaexapi の Google サービスアカウント秘密鍵、alphaaimail の Google OAuth client_secret。alphaaimail の Anthropic APIキーはデモ終了時に削除済み
- **未対処**: `.env` / `config/credentials.json` を Git 追跡から外す作業(`git rm --cached` + `.gitignore` 整備)

### 判明した現状(設計書の誤りを訂正)

タスク0 spec に「alphasyscdk には既に RDS 用の Secrets Manager 利用例があるためパターンを流用できる」と記していたが、**誤りだった**。

- 実際は CloudFormation パラメータ(`CfnParameter`、`noEcho: true`)を使用。**Secrets Manager もパラメータストアも現状どこにも導入されていない**
- 機密の流れ: デプロイ実行者のローカル環境変数 → CFnパラメータ → 文字列連結 → **ECS タスク定義の `environment` に平文**(`alphasyscdk/lib/constructs/ecs.ts`)
- 平文がタスク定義に載るため、AWSの参照権限がある人全員に見える
- **根本原因**: デプロイ実行者のローカル `.env` が事実上の原本になっていること。だから各リポジトリに `.env` が必要になり、コミット事故につながった
- 訂正は spec / plan / aimail study の3ファイルに反映済み

### 一括管理方針(検討結果)

| 対象 | 保管先 | ローカル開発 |
| ---- | ---- | ---- |
| APIキー類(BANKCODE、Anthropic 等)、OAuth Client Secret | **SSM パラメータストア(SecureString)** | `.env`(生成物) |
| DB接続情報 | **Secrets Manager**(RDS自動生成) | `.env` の `DATABASE_URL` |

- パラメータストアを選ぶ理由: **無料**(Standard)なので項目を細かく分けられ、パス単位(`/alpha/<環境>/<プロジェクト>/<キー>`)でIAM権限を切れる。Secrets Manager は項目ごとに課金されるためコスト回避でJSONに詰め込むことになり、項目単位の権限分離ができなくなる
- **本番では `.env` を使わない**。ECS タスク定義には値ではなくパラメータのARNを書き(`ecs.Secret.fromSsmParameter` / `fromSecretsManager`)、ECSが起動時にIAM権限で取得して環境変数として注入する。**平文がタスク定義に載らなくなる**
- **アプリのコードは変更不要**。ローカルも本番も `process.env.X` を読むだけで、違いは「誰が環境変数を用意するか」のみ
- ローカル用の `.env` 生成コマンドは**開発用パス(`/alpha/dev/...`)を既定**とし、本番の値をローカルに降ろせないようにする(降ろす仕組みを作ると同じ事故が形を変えて再発する)
- **再発防止をセットにする**: `gitleaks` を pre-commit フックに入れる。GitHub のシークレットスキャンは private リポジトリでは GHAS 契約が必要なため、ローカルフックが実用的

### DB接続情報の扱い(方針確定)

**環境変数に `DATABASE_URL` があればそのまま使い、無ければ部品から組み立てる。**

```
if (process.env.DATABASE_URL) return process.env.DATABASE_URL;   // ローカル
return compose(DB_USER, DB_PASSWORD, DB_HOST, DB_PORT, DB_NAME); // 本番/ステージング
```

- 部品は**アプリがSDKで取得せず、ECSに注入させる**(`ecs.Secret.fromSecretsManager(secret, 'password')` 等)。アプリにAWS SDKを持ち込まず、VPCエンドポイントも起動時API呼び出しも不要になる
- **この形を最初に入れておくことが要点**。後から自動ローテーションを有効にする際、`addRotationSchedule()` の追加とVPCエンドポイントの用意だけで済み、**アプリのコードもURL形式も触らない**。逆に `DATABASE_URL` 1本で持つ形にすると、後でローテーションを入れる際にアプリ変更が必要になる

### 「Prisma は Secrets Manager に合わないのか」への回答

**合わないわけではない。** 当初4つの落とし穴を並べたため誤解を招いたが、原因を切り分けると次のとおり。

| 落とし穴 | 本当の原因 | Prisma固有? |
| ---- | ---- | ---- |
| 1. ローテーション時に稼働中タスクが古い値を持つ(再起動が必要) | コネクションプール + ECSの注入タイミング | いいえ |
| 2. `prisma migrate` は `DATABASE_URL` しか読めない | Prisma CLI の仕様 | **はい**(エントリポイント1行で解決) |
| 3. 自動生成パスワードの記号でURLが壊れる | DSN文字列形式全般 | いいえ |
| 4. 既存DBのマスターパスワードが変わる | 既存CFn構成からの移行 | いいえ |

- **Prisma固有は2番だけ**。1番と3番は単一DSN文字列を受け取る仕組み全般の問題(Rails の `DATABASE_URL` 等でも同じ)
- **本当の論点は「自動ローテーションを今入れるか」**。1番とローテーションLambdaのVPCエンドポイント/NAT要件は、ローテーションを入れなければ発生しない
- **RDSは未稼働**と確認できたため、**4番は完全に消滅**。最初から `Credentials.fromGeneratedSecret()` で作れる
- 3番の対策は両面から: 生成側で `excludeCharacters: '/@":\\\'?#%&+ '`、アプリ側で `encodeURIComponent`。生成側だけに頼ると手動設定値で事故る

### 未決事項

- [ ] **自動ローテーションを最初から有効にするか、後回しにするか**(検討中)
  - 後回し: `fromGeneratedSecret` で平文を排除する効果は今すぐ得られ、落とし穴1とVPCエンドポイント要件が発生しない。有効化は後からインフラ側だけで可能
  - 最初から: `alternating users` 戦略(前回の資格情報が1周期有効に残る)+ ローリング再起動 + VPCエンドポイントの設計が必要。`single user` 戦略は即時切替のため再起動までダウンする
- [ ] `.env` / `config/credentials.json` の Git 追跡解除と `.gitignore` 整備の実施
- [ ] `.env.example` の整備(現状 alphaaimail と alphadb のみ存在。alphasys / alphaexapi には無い)

## 2026-07-28 シークレット管理 設計書の作成とローテーション構成の確定

詳細は [design/2026-07-28-secret-management-design.md](design/2026-07-28-secret-management-design.md)。

### 確定したローテーション構成

| 対象 | ローテーション | 戦略 | 用途 |
| ---- | ---- | ---- | ---- |
| マスターユーザー | **自動** | `single user` | ローテーションLambda、`prisma migrate`、DBA作業 |
| アプリユーザー | **自動** | `alternating users` | アプリ(alphasys / alphaexapi)の接続 |

- **前回の助言を訂正した**。前回「マスターは手動ローテーション」としたが、**手動の方がリスクが高い**。DBのパスワードだけ変更して Secrets Manager 側の更新を忘れると、マスターシークレットを使うアプリユーザーの自動ローテーションが**静かに失敗し続ける**。自動化すればこの同期漏れは構造的に発生しない。
- **マスターの自動ローテーションはアプリに影響しない**。アプリが持つのはアプリユーザーの資格情報のみで、マスターを使うのはローテーションLambda・マイグレーション・DBA作業に限られ、いずれも短命または都度取得のため。
- **マスターに `alternating users` が使えない理由**: この戦略は「別の特権資格情報(`masterarn`)を使って対象のパスワードを変更する」仕組みで、対象がマスター自身だと渡すべき特権シークレットが存在しない。またユーザーを複製するため、スーパーユーザーが2つできてしまう。`single user` ならマスター自身でログインして自分のパスワードを変更するだけなので成立する。

### 再起動のタイミング(アプリユーザー)

- **AWSに組み込みのトリガーは存在しない**。ECSがシークレットを解決するのはタスク起動時のみで、ローテーション後も稼働中タスクは古い値を持ち続ける。
- `alternating users` では**前世代の資格情報が1ローテーション周期分有効に残る**ため猶予がある。周期90日なら「1周期に1回以上タスクが入れ替わっていればよい」。
- **方針: 通常のデプロイに便乗する(自動化しない)**。周期90日に対しデプロイが月1回程度あればマージンは十分で、EventBridge や Lambda を増やさない分「静かに壊れる」箇所も増えない。
- 必要になった場合の選択肢: ①ローテーション連動(EventBridge → Lambda → `forceNewDeployment`。2026年7月から Secrets Manager が EventBridge へ直接イベント発行するようになった) ②定期実行(週次の強制デプロイ。ローテーションに依存せず**パラメータストア側の変更も反映できる**ため使い道が広い)
- **注意点**: PostgreSQL はパスワード変更時に既存接続を切らないため、猶予を過ぎても**新規接続が必要になった瞬間だけ失敗する**。断続的な認証エラーとして現れ原因が分かりにくい。CloudWatch アラームが必要。

### 調査で解決したタスク0の未確定事項

**ECSタスク定義へのシークレット注入経路が判明した。** `alphasyscdk/lib/constructs/ecs.ts` の1箇所に集約されており、**同一タスク定義に alphasys(Frontend、3000番)と alphaexapi(Backend、3001番)が同居**している。`alphaexapi/infrastructure.yml` は CI/CD 用(ECR・CodeBuild・IAM)でタスク定義ではなかった。タスク0 spec / plan の該当箇所を解決済みに更新した。

### 確認が必要な事項(設計書12章)

- **マスターパスワードのコピーが Secrets Manager の外に存在しないか**(CodeBuild/CIの環境変数、`prisma migrate` の実行方法、開発メンバーのローカル `.env`)。存在するとそこだけ古い値で取り残されて壊れる
- アプリユーザーに与える権限の設計(現状アプリはマスター権限で接続。マイグレーション=マスター、アプリ=DML のみとする想定)
- ローテーションLambdaのネットワーク要件(Secrets Manager の VPC エンドポイント、または NAT 経由の外向き通信が必要)
- alphabatch の実行基盤(`ecs.ts` のタスク定義に含まれないため注入経路が別にあるか)

### 検討した代替案(7.6 に追記): マスターを2つ用意して相互にローテーションする案 — 採用せず

「マスターを2つ(`master_a`/`master_b`)作り、`master_a` のローテーションは `master_b` の資格情報で行う」という構成を外部記事で提示され、見解を求められた。**single user では Lambda が「今から変更するパスワード」でログインするため、処理中断でロックアウトされ詰む**という懸念に基づくもの。

懸念自体は実在するが、以下により採用しないと判断した。

1. **RDS のマスターパスワードは常に復旧できる** — `modify-db-cluster --master-user-password` で強制リセット可能。最悪でも「手動リセット + シークレット同期」で済み、**恒久的に詰まない**。RDS 自体が上位の管理者として振る舞えるため、一般ユーザーのロックアウトとは性質が異なる
2. **AWS の機構に回復手段が組み込まれている** — 旧バージョンが `AWSPREVIOUS` として1周期保持され "last known good" になる。またローテーション関数は `AWSCURRENT` でのログイン失敗時に `AWSPENDING` を試す(前回試行でDB側だけ変更済みのケースの回復)
3. **この構成は AWS の MultiUser(alternating users)ではない** — AWSのそれは対象ユーザーを**複製**し**固定の管理者シークレット**で変更する非対称な仕組みで、`master_b` は使われない。記事の対称構成を実現するには**カスタムLambdaの自作が必要**で、上記2の回復機構も自前実装になる
4. **リスクの総量が減らない** — スーパーユーザーが2つになりセキュリティ上は後退。かつ相互依存が生まれ、「自己変更のリスク」を「相互依存のリスク」に置き換えただけ
5. **AWS の推奨と整合しない** — AWSは single user を「最も単純でほとんどのユースケースに適切」とし、特に「**一時的(アドホック)または対話的なユーザー**に推奨」。本設計のマスターの使い方はこれに該当する。またAWSがロックアウト防止として推奨するのは「別の管理者シークレットでアプリユーザーのローテーションを担わせる」構成であり、**本設計の alternating users がまさにそれ**

なお公平を期すため、この代替案が正当化される条件(マスターを長期稼働アプリが常用/RDS以外で上位リセット手段が無い/ローテーション頻度が高い)も設計書に併記した。**本設計は「アプリにマスターを使わせない」ことでこれらの前提を消している。**

## 2026-07-29 機密ファイルの追跡解除とローテーション完了

### 実施内容(4リポジトリ、いずれもマージ済み)

| リポジトリ | PR | 追跡解除したファイル |
| ---- | ---- | ---- |
| alphasyscdk | #17 | `.env` / `.env.dev` / `.env.prod` / `.env.staging` |
| alphaaimail | #2 | `.env` / `config/credentials.json` / `config/token.json` |
| alphasys | #132 | `.env` / `.env.test`(+ README の平文除去) |
| alphaexapi | #43 | `.env` / `.env.development` / `.env.production` / `config/credentials.json` |

各リポジトリに共通で追加したもの: `.gitignore` 整備、`.env.example`、`.githooks/pre-commit`(gitleaks)、`docs/SECRETS.md`。

**ローテーションは対象すべて完了**(2026-07-29 時点)。

### 作業中に新たに見つかった機密

- **`alphaaimail/config/token.json`** — 生きた OAuth の access_token / refresh_token。当初の調査で挙げていなかった
- **`alphasys/README.md`** — 88行目に接続URL内のパスワード、107行目に `AUTH_GOOGLE_SECRET`、**108行目に `AUTH_SECRET`**。当初は107行目のみ報告していたが、実際は3箇所あった。README は追跡対象のままなので、`.env` の追跡解除では解決せず個別に置換が必要だった
- `alphasyscdk/.env` の `AUTH_GOOGLE_SECRET` と `GOOGLE_CLIENT_SECRET` は**同一の値**だった(同じ OAuth クライアントをフロント/バックで共用)

### 方式を husky から `core.hooksPath` に変更した

当初 husky を予定したが、**alphaexapi は `npm install` に CodeArtifact 認証が必要**で、依存追加と lockfile 更新を確実に行えなかった。`core.hooksPath` は husky が内部で使う仕組みそのもので、**依存を増やさず4リポジトリを同一の形にできる**ため採用した。

代償として、クローンごとに `git config core.hooksPath .githooks` の実行が必要になる(Git の仕様上、リポジトリに含めるだけでは自動有効化されない)。`gitleaks` 未インストール状態で有効化するとコミットが止まるため、**`brew install gitleaks` → `git config` の順**で実施する。

**フックの実動作は未検証**(作業環境に gitleaks が未インストールのため、シェル構文チェックのみ)。

### 運用上の注意: 追跡解除したファイルはブランチ切替で消える

`git rm --cached` はワーキングコピーを残すが、**その後にブランチ切替や `git pull` で削除コミットを取り込むと、Git がワーキングコピーを削除する**(切替時点でファイルが追跡状態だったため Git の管理対象になる)。

今回この操作順で alphasyscdk と alphaaimail の7ファイルを実際に失い、履歴から復元した。復元方法:

```bash
C=$(git log --diff-filter=D --format=%H -1 -- .env)
git show "$C^:.env" > .env
```

**alphasys / alphaexapi は `feature/migrate-to-alphadb` 作業中のため `.env` がまだ追跡されている。** main のマージまたは切替時に同じ現象が起きるので、事前にバックアップが必要。

### 残っている確認事項

- ~~マスターパスワードのコピーが Secrets Manager の外に存在しないか~~ → **解消(2026-07-29)**。**マスターパスワードは一切使用されていない**(RDS が未稼働のため、`alphasyscdk/.env` に設定されていただけで実際には使われていない)。よって取り残される古い値は存在せず、CodeBuild/CI・`prisma migrate`・ローカル `.env` の確認はいずれも不要
  - **移行時の利点**: 既存の資格情報を引き継ぐ必要がなく、`fromGeneratedSecret` の生成値をそのまま使える。既存接続の切り替え調整も発生しない。アプリユーザーとマスターの権限分離も、既存構成からの移行ではなく**最初からその形で作れる**
- gitleaks フックの実動作確認(各自の環境で `brew install gitleaks` 後)

## 2026-07-29 シークレット管理移行の実装計画と登録スクリプト作成

詳細は [design/2026-07-29-secret-management-migration-plan.md](design/2026-07-29-secret-management-migration-plan.md)。

### 判明した前提(着手前の調査)

- **私の AWS 認証情報はデプロイ対象と別アカウントを指している**。alphasyscdk のデプロイ対象は `938812705475` だが、作業環境の認証情報は `570024666076`(研修環境と思われる、管理者権限)。**したがってパラメータストアへの登録(手順1)は実施できない**ため、登録スクリプトを用意して実行はお客様にお願いする形にした
- 現在のアカウントに社内システムの RDS・ECS スタックは存在しない(`NetworkStack` / `KeyPairsStack` / `alphasys-launch-templates` / `CDKToolkit` のみ)。RDS未稼働と整合
- **ネットワーク要件が解決した**: DBインスタンスは `PRIVATE_ISOLATED` だが VPC は `natGateways: 2` を持つ。**ローテーションLambdaを `PRIVATE_WITH_EGRESS` に置けば NAT 経由で Secrets Manager に到達でき、VPCエンドポイントは当面不要**。ただし既定ではクラスタと同じサブネットに置かれて失敗するため、`vpcSubnets` の明示指定が必須
- **機密の平文が4箇所目に見つかった**: `migrate-codebuild.ts:43` で `DATABASE_URL` を CodeBuild の平文環境変数として渡していた。CodeBuild の設定を見られるとパスワードが読める。移行対象に追加した
- 両アプリは `new PrismaClient()` を引数なしで呼んでおり `DATABASE_URL` を環境変数から読むだけ。マイグレーションは CodeBuild で `npx prisma migrate deploy` を実行している

### 登録スクリプト(`scripts/register-secret-parameters.py`)

手順1用。安全のための仕様を入れた。

- **既定はドライラン**。`--apply` を付けない限りAWSへ書き込まない
- **`--expect-account` でアカウントを照合**し、一致しなければ中止する。`--apply` 時は必須(今回のようなアカウント相違による誤登録を防ぐため)
- **値は一切標準出力に出さない**(文字数のみ表示)
- 登録対象は機密7件のみ。非機密の設定値(リージョン・ポート・CIDR・コンテナ名等)は `.env` のまま扱い、DB資格情報は Secrets Manager が自動生成するため対象外

動作確認済み(ドライラン成功、`--apply` 単体では安全装置が働いて中止)。

### 実装計画の構成(7タスク)

| Task | 内容 | リポジトリ |
| ---- | ---- | ---- |
| 1 | `buildDatabaseUrl()` の追加 | alphadb |
| 2 | RDS資格情報を Secrets Manager 自動生成へ | alphasyscdk |
| 3 | ECSの機密注入を `environment` → `secrets` | alphasyscdk |
| 4 | ローテーション追加(master=single / app=alternating) | alphasyscdk |
| 5 | マイグレーションCodeBuildの平文排除 | alphasyscdk |
| 6 | アプリを `buildDatabaseUrl` に切替 | alphasys / alphaexapi |
| 7 | デプロイと検証 | — |

### 設計上の判断

- **`buildDatabaseUrl()` は alphadb に置く**。alphasys と alphaexapi の両方が既に alphadb に依存しており、**資格情報の組み立てロジックを2箇所に複製すると差異が生じる危険がある**ため単一実装にする。代償として alphadb の publish が Task 6 の前提になる
- **アプリユーザーはマスターとは別ユーザーにする**。現状アプリはマスター権限で接続する設計だったが、RDS未稼働のため**最初から権限分離した形で作れる**。マイグレーション(DDL)はマスター、アプリは DML のみ
- **`appUserSecret` はシークレットを作るだけでDB上のユーザーは作られない**。デプロイ後に一度だけ `CREATE USER` と `GRANT` を実行する手順を Task 7 に含めた

### 確認が必要な事項

- alphadb に TypeScript のビルド機構があるか(`src/database-url.ts` のコンパイルと公開に必要)
- alphadb の publish 可否(Task 6 の前提。外部影響のある操作なので事前確認)
- alphabatch のシークレット注入経路(`ecs.ts` のタスク定義に含まれていない)
- CodeBuild で `jq` が使えるか(URLエンコードに使用)

## 2026-07-30 DB同期(本番→ステージング)の要件を確認(実装は保留)

alphasyscdk の `staging` に main をマージした際、`DbSyncStack`(本番→ステージングへのAurora clone同期)が本番/ステージングを同一アカウント前提で直接参照する設計であることが判明し、現行のアカウント分離方針と相容れないためクロスアカウント対応の再設計が必要と判断した(→ alphasyscdk PR #20)。再設計は「落ち着いてから」対応するとのことで、要件のみ先に確認した。

**要件**:
- 通常は深夜に本番DBをステージングDBへコピーする(定期実行)。
- ステージングDBは通常コピー時のみ起動し、コピー完了後は停止する(常時起動しない)。
- ステージングで特別作業中は「同期停止フラグ」を立てておくと、同期を行わない。この間はステージングDBの起動・停止も行わない(基本的に起動しっぱなしのまま)。

**次のアクション**: クロスアカウント対応の設計に着手する際、上記3点(定期同期・DB自動起動停止・作業中の同期停止フラグ)を要件として満たす形で再設計する。

## 2026-07-30 ブランチ運用ルールの明文化と、staging 取り残しの発覚

詳細は [design/2026-07-30-branch-strategy.md](design/2026-07-30-branch-strategy.md)。

alphasyscdk 側で alphadb の CodeBuild が失敗した件を調査中、alphadb の `main`/`staging` が完全一致していることに気づき、ブランチ運用について確認したところ、全プロジェクト共通のルールが判明した。

- 全プロジェクトで `main`/`staging` の2ブランチを維持する（alphadb は staging のみで足りる、alphacdk/alphasyscdk も環境分離自体は不要だが、他プロジェクトと**平仄を合わせるため**両方維持）。
- 通常運用は `main` = `staging`。変更は **`staging` からブランチを作成し `staging` にマージ**（push/マージが CI ビルドのトリガー）。**本番リリース時のみ** `staging` → `main`（マージが本番アプリのローリングアップデートのトリガー）。

**判明した逸脱**：このルールが伝わっていなかったため、Claude がこれまで `main` からブランチを作成し `main` に直接マージする運用をしており、結果として以下が `staging` が数ヶ月分取り残された状態になっていた。

- alphasyscdk: staging が 2026-04-29 で停止(main比47ファイル差分)
- alphasys: staging が 2026-04-16 で停止(main比153ファイル差分)
- alphaexapi: staging が 2026-04-16 で停止(main比45ファイル差分)

alphacdk/alphadb は `main`=`staging` を維持できており問題なし。**既存の乖離解消（staging の最新化）は未着手**、今後の対応が必要。

また alphasys/alphaexapi に残る `dev` ブランチ(最終更新 2025-12-06)は、本ルール確立前の残骸で現在未使用。設計書に「削除検討可」と記載した。

## 2026-07-30 staging取り残しの解消と、staging→mainのPR自動化という課題の発見

alphasyscdk / alphasys / alphaexapi の3リポジトリとも、`main` を `staging` にマージして乖離を解消した(各リポジトリの `feature/merge-main-into-staging` PR、マージ済み)。あわせて alphadb 側でも移植漏れのスキーマ変更(alp_skill_additionals へのカラム追加)を別PRで対応。

マージ作業では、**git の自動マージがコンフリクトとして検出せず一方の変更(主に main 側)を無言で破棄するケースが多数見つかった**(alphasyscdkではECRやGitHub接続の RemovalPolicy が意図せず DESTROY に戻る等、実害のある事例も)。そのため、コンフリクトマーカーだけを信頼せず、両ブランチの全差分ファイルを手動で突き合わせて確認する方針で対応した。alphasys/alphaexapi では大半が「staging が main より後から同じ機能を発展させたもの」で実害は無かったが、alphadb 一元化(A-2)へのフィールド名/enum リネーム追従漏れ等は実際に修正が必要だった。

**新たな課題として次を記録**：`staging` へのマージを契機に `staging` → `main` のPRを自動作成する仕組みが欲しい。今回のような「`staging` が `main` から数ヶ月分取り残される」事態の再発防止になる。詳細は [design/2026-07-30-branch-strategy.md](design/2026-07-30-branch-strategy.md) の「6. 課題」に記載。

## 2026-07-30 alphadb の `main` ブランチの位置付けに関する留保

alphadb-publish CodeBuild が対象ブランチを明示指定していない（デフォルトブランチ＝`main`）と判明したのを受けて、alphadb の `main`/`staging` の役割を再確認した。

- alphasys/alphaexapi の `main` は「push → 本番サービスのローリングアップデート」という実際の役割を持つ。
- 一方、alphadb の唯一のリリース動作は npm publish で、これは「Artifact は共有アカウント（ステージング）側で作る」という他の運用方針に厳密に合わせるなら `staging` を対象にすべきもの。しかしそうすると、alphadb の `main` は**他プロジェクトとのブランチ命名を揃える以外の意味を持たなくなる**。
- **結論**：矛盾は認識した上で、今回は変更せず保留とする。alphadb-publish の対象ブランチもこのまま（デフォルトブランチ＝`main`）にしておく。設計書（[design/2026-07-30-branch-strategy.md](design/2026-07-30-branch-strategy.md) 2章）に留保として記載した。

## 2026-07-30 alphadb staging ブランチの誤削除・復元と、main/staging 削除保護の検討

alphadb-publish CodeBuild の調査中に、`origin/staging` が PR#8（CodeArtifact認証トークン修正）を含まない古い状態に戻っていることに気づいた。`origin/main` には PR#8 の内容が含まれていたため、`staging` だけが取り残されているように見えた。

確認したところ、**main へのマージ後に alphadb の `staging` ブランチを削除し、その後 GitHub の restore 機能で復元していた**ことが判明。復元自体は成功しており、`main`/`staging` は再fetch後に完全同期していることを確認した(実害なし)。

再発防止として `main`/`staging` に GitHub のブランチ削除保護を掛けることを検討したが、**GitHub organization(alphacmc)が Free プランのため、プライベートリポジトリではブランチ保護機能(削除保護含む。Rulesets・classic protection どちらのAPIも403)が使えない**と判明。

- **結論**：GitHub Team 等への課金プラン変更の決裁が下りるまでは、技術的な強制はできないため、**運用ルールとして「main/stagingを直接削除しない」を明文化するのみ**とする(→ [design/2026-07-30-branch-strategy.md](design/2026-07-30-branch-strategy.md) 7章)。決裁が下りたら該当5リポジトリ(alphasyscdk/alphacdk/alphasys/alphaexapi/alphadb)に削除保護を設定する。

## 2026-08-06 シークレット管理 Task 6 の実施と、alphadb のバージョン運用の確定

> この記録は当日ローカルにコミットしたまま push し忘れており、後日(2026-08-24)に追記した。そのため下記 2026-08-19 の棚卸しは本記録を踏まえずに行われている。**2026-08-19 の記録にある「シークレット管理移行は alphasyscdk 側のみ実装済み」は不正確で、アプリ側(Task 6)も本日完了・マージ済み**である(→ [移行計画の実施状況](design/2026-07-29-secret-management-migration-plan.md))。

### 発端: デプロイしても DB に接続できない状態だった

移行計画の Task 1〜5 が完了する一方 **Task 6(アプリ側の切替)が未着手**で、次の不整合が残っていた。

- ECS のタスク定義は `DB_USER`/`DB_PASSWORD`/`DB_HOST`/`DB_PORT`/`DB_NAME` の5部品を注入する形に変わっていた
- 一方アプリは `new PrismaClient()` を引数なしで呼んでおり **`DATABASE_URL` しか読まない**
- その `DATABASE_URL` はもう注入されないため、デプロイしても接続に失敗する状態だった

### 対応範囲は「アプリのみ」だった

「アプリ / CI・CD / ローカル」の3面を確認したところ、**手を入れる必要があったのはアプリ側だけ**だった。

| 面 | 供給方法 | 対応 |
| --- | --- | --- |
| アプリ実行時 | `DB_*` 5部品を appUserSecret から注入 | 要変更（`buildDatabaseUrl()` を呼ぶ） |
| alphasys の Docker ビルド | 無し | 要変更（ビルド用ダミー1行） |
| `prisma generate`（alphadb publish） | buildspec の `env.variables` にダミー | 対応済み |
| `prisma migrate deploy` | masterSecret の5部品を注入し buildspec で組み立て | 対応済み（Task 5） |
| ローカル | `.env` の `DATABASE_URL` | 変更不要 |

**`prisma generate` にダミーが要る理由**は Prisma 一般の仕様ではなく、alphadb が `prisma.config.ts` を使っており `env("DATABASE_URL")` が**設定ファイルの読み込み時点で評価される**ためである。設定ファイルは CLI の全サブコマンドで読まれるので、DBに接続しない `generate` でも値が必須になる。publish ビルドはDBに触れないため、**このダミーは実資格情報に置き換えない**（置き換えれば平文の在り処が1箇所増えるだけ）。

### alphadb のバージョン運用: 開発初期は `1.0.0` を上書きし続ける

**方針(確定)**: 実運用のメンテナンスでは当然に版数を上げるが、**開発初期の間は `1.0.0` のまま上書き publish を繰り返す**。

障害となっていたのは、buildspec に「同一バージョンが既に存在すれば publish をスキップ」する処理が**初期コミット(2026-06-16)から入っていた**こと。`staging` に push しても CodeBuild は publish せずに終わっており、この運用と矛盾していた。CodeArtifact は既存バージョンへの上書きを拒否するため、`delete-package-versions` で削除してから publish する形に変更した(alphadb #12)。

- **⚠️ 削除処理と `codeartifact:DeletePackageVersions` 権限(alphasyscdk #48)は、いずれも「開発初期のみ」の措置。** 本番リリース後にこれらが残っていると、CI がリリース済みバージョンを消せてしまう。版数を上げる通常運用に切り替える時点で、buildspec の削除ブロックと IAM 権限の両方を外すこと。コード内にもコメントで明記した
- 削除は publish 直前(post_build)に置いた。pre_build で消すと、後続フェーズが失敗したときに「消しただけで publish されていない」空白状態が残るため

### 訂正: 同一バージョンを上書きしても integrity は変わらない

当初「上書きすると `npm ci` が EINTEGRITY で落ちるので `package-lock.json` の更新が必要」と判断していたが、**実測の結果これは誤りだった**。

npm はパッケージ tarball 内の mtime を正規化するため、**同じ内容を再 publish すればバイト単位で同一の tarball が生成され、ハッシュも変わらない**。実際に `alphadb@1.0.0` を削除して publish し直した後も、alphasys / alphaexapi の lockfile の alphadb エントリに変更は発生しなかった。EINTEGRITY が問題になるのは**中身を変えて同一バージョンで上書きしたとき**だけである。

### この日つまずいた点(再発しやすいもの)

- **CDK はマージだけでは反映されない。** alphasyscdk #48 をマージしても IAM 権限は変わらず、CodeBuild は同じ `AccessDeniedException` で失敗し続けた。`cdk deploy` が必要。alphasyscdk には自動デプロイの CI が無い(`buildspec-example.yml` のみ)
- **2025年付の古い生成物が `tsc` を壊していた。** `lib/cicd-stack.ts` は削除済みなのに `cicd-stack.d.ts` / `.js` が残り、改名前の `CodeBuildConstruct` を参照して `npm run build` が失敗した。`.gitignore` 対象の未追跡ファイルなので削除で解消。`cdk diff` は ts-node 実行のため通り、`tsc` で初めて露見した。恒久対策として `outDir` の設定か `tsc --noEmit` 化が有効
- **ローカルからの CodeArtifact 操作にはトークンの export が必要。** `.npmrc` は `_authToken=${CODEARTIFACT_AUTH_TOKEN}` の環境変数参照なので、`aws codeartifact get-authorization-token` の結果を毎回シェルに設定しないと 401 になる(既定12時間で失効)

### この日マージしたもの

| リポジトリ | PR | 内容 |
| --- | --- | --- |
| alphadb | #12 | buildspec を同一バージョン上書き対応へ |
| alphadb | #14 | データ移行クエリ一式(11ファイル)の保全 |
| alphasyscdk | #48 | `codeartifact:DeletePackageVersions` 権限追加(SharedStack デプロイ済み) |
| alphasys | #137 | `buildDatabaseUrl()` への切替 + Dockerfile のビルド用ダミー |
| alphaexapi | #49 | `buildDatabaseUrl()` への切替 |

### 未確認のまま残っている2件

- **alphadb-publish の CodeBuild が delete→publish で成功したか。** SharedStack のデプロイ直後に再実行しないまま中断した。その後 Track1 で alphadb の再 publish が行われているはずなので通っている可能性が高いが、未確認
- **alphasys の Dockerfile のダミーが本当に必要か。** `env -u DATABASE_URL npm run build` で確認できる。不要と分かればダミーは外してよい

## 2026-08-19 社内システム全体の作業棚卸し(輻輳解消のための整理)

**きっかけ**: 複数プロジェクト・複数イニシアチブが並行し、ユーザーから「作業が輻輳して、どこから手をつけて良いかわからなくなった」との相談。ユーザーの希望粒度は「全体像＋着手優先順位の提案まで」、対象範囲は「CLAUDE.md構成表の8プロジェクト＋alphabsmail」。全プロジェクトのgit状況(ブランチ・未コミット・オープンPR)を機械的に確認し、alphasystem/docs、alphabsmail/docs、alphasyscdk等の設計ドキュメントを3並列のExploreエージェントで読み込んで現状を突き合わせた。

### すぐ片付く整理事項(輻輳感を直接減らせるもの)

- alphasys/alphaexapi/alphadb: `feature/track1-sales-mail-visualization` が2026-08-17に各リポジトリでstagingへマージ済みなのに、ローカルはそのブランチをチェックアウトしたまま(3リポジトリとも未マージのローカル変更なし・fetch後も差分なしを確認済み)。`staging`に切り替えてfeatureブランチを削除してよい状態。
- alphasys #139・alphaexapi #51: `staging`→`main`のPR(2026-08-17作成、MERGEABLE、Draftではない)が未処理のまま放置。本番リリースするかどうかの判断待ち。
- alphasystem直下・alphabatchに`.DS_Store`が未追跡のまま散在。`.gitignore`対応推奨。
- alphaaimailに`docs/table-design.md`という未コミットの新規ファイルが残っている。

### 大型イニシアチブごとの現状

1. **営業メール可視化Track1(alphabsmail)**: 実装9タスクは実質すべて完了・マージ済み。残るのは(b) alphaexapiの実デプロイ環境(ECS Secrets Manager等)への`SALES_MAIL_IMAP_*`・`ANTHROPIC_API_KEY`設定と、(c) 実環境での結合確認(plan.md末尾項目5〜7)。いずれも認証情報を伴う運用作業のためAIは代行せずユーザー対応待ち。加えてTask8「AI推測がNoの場合の候補選択UI」は次イテレーション送りのまま、plan.mdのTask1テンプレートがdesign v5に追随できていない同期漏れも残っている。
2. **MFクラウド連携**: 設計(ロードマップ・タスク0 spec/plan)は完了しているが実装は未着手。タスク0(API連携基盤)がタスク1〜4・aimail統合フェーズ2の両方の前提になっており、事実上のボトルネック。
3. **シークレット管理移行**: 設計書・移行計画書上のチェックボックスは全て未チェックのままだが、実際には**alphasyscdk側で本体(RDS Secrets Manager化・ローテーション・ECS secrets注入・migrate-codebuildの平文排除)がPR#18等で実装・マージ済み**。ドキュメントが実態に追随していない乖離があり、次回チェックボックスの棚卸し・更新が必要。
4. **alphaaimail統合**: alphaaimail本体に「非推奨」の明示宣言はないが、2026-07-29以降コミットが止まっており、後継のalphabsmail(Track1)に実質置き換わりつつある段階。正式な廃止/アーカイブの意思決定はまだされていない。
5. **ブランチ運用ルール**: 確定済み・運用中。残課題は「staging→mainの自動PR化」(未着手)と「main/staging削除保護」(組織の課金プラン変更の決裁待ちで保留合意済み)。
6. **DB同期(DbSyncStack)クロスアカウント対応**: 要件確認のみ済み、実装は「落ち着いてから」として保留合意済み。

### 提案した優先順位

1. リポジトリ整理(すぐ片付く事項)を先に片付け、輻輳感を減らす。
2. Track1の残タスク(b)(c)はほぼ終わっているため、ユーザーに認証情報設定・結合確認の対応を促して早期にクローズする。
3. MFクラウド タスク0(API連携基盤)は複数イニシアチブのボトルネックなので、着手するかどうかを次の大きな意思決定として扱う。
4. シークレット管理の設計書・移行計画書のチェックボックスを実態(alphasyscdkの実装済み範囲)に合わせて更新する。
5. alphaaimailの扱い(廃止/アーカイブ)を正式に決める。
6. staging→main自動PR化・削除保護・DB同期再設計は、合意通り保留を継続(緊急度低)。

**現在のステータス**: 上記の整理結果をユーザーに提示。次にどれから着手するかはユーザーの判断待ち。

## 2026-08-19 リポジトリ整理の実施、Track1残作業の再開(シークレット注入のCDK対応)

**リポジトリ整理の実施結果**:

- alphasys/alphaexapi/alphadb: `staging`へ切替、マージ済みの`feature/track1-sales-mail-visualization`をローカル削除(リモートは各リポジトリのマージ時に自動削除済みで確認)。
- alphasystem: `feature/organize-repos-and-notes`でCLAUDE.mdの「作業エージェント」節・本棚卸し記録・`alphabsmail/`の格納・ルート`.gitignore`新設をコミット、Draft PR #19を作成。ユーザーが確認しマージ済み。
- alphabatch: `feature/ignore-ds-store`で`.gitignore`に`.DS_Store`追加、Draft PR #17。ユーザーがマージ済み。
- alphaaimail: `feature/add-table-design-doc`で未コミットの`docs/table-design.md`をコミット、Draft PR #3。ユーザーがマージ済み。
- alphasys #139・alphaexapi #51(`staging`→`main`)は本番リリースを意味するためAIはマージせず保留していたが、**ユーザーが確認の上マージ済み**(Track1のコードが`main`に反映され、本番デプロイがトリガーされた状態)。

**Track1残作業の再開にあたっての追加調査**: `main`マージにより本番デプロイがトリガーされた可能性を踏まえ、Track1が必要とするシークレット(`SALES_MAIL_IMAP_HOST`/`PORT`/`USER`/`PASSWORD`・`ANTHROPIC_API_KEY`)の本番側の設定状況を調査した結果、**alphasyscdk(ECSタスク定義)・`register-secret-parameters.py`のいずれにも対応が一切無い**ことが判明した。

- コード上は`getOrThrow`等のランタイムガードが無いため、未設定でもアプリ起動・プロセスはクラッシュしない。ただしIMAP取り込み(`mail-polling.job.ts`、10分毎`@Cron`)・AI推測(`contract-suggestion.service.ts`)は、`try/catch`で例外を握りつぶして`logger.warn`に残すだけで、機能としては常に黙って失敗し続ける状態(design v5・plan.mdの想定通りの挙動ではあるが、機能自体は動かない)。
- ユーザーに確認の上、まずCDK側のコード対応から着手する方針とした(Task8「Noの場合の候補選択UI」の追加実装は今回のスコープ外とし、先送りを継続)。

**対応(コードのみ。実値の登録・デプロイはユーザー側)**:

1. alphasyscdk: `lib/constructs/ecs.ts`の`backendSecrets`に、既存の`BANKCODE_API_KEY`等と同じ`secureParam()`パターンで上記5項目を追加。`feature/sales-mail-secrets`ブランチ、Draft PR #50。
2. alphasystem: `scripts/register-secret-parameters.py`の`MAPPING`に同5項目を追加(参照元は`alphaexapi/.env.{env}`)。`feature/sales-mail-secrets-mapping`ブランチ、Draft PR #21。

**残っているユーザー側の作業(認証情報を伴うためAIは代行しない)**:

- `alphaexapi/.env.staging`(および`.env.prod`)に実際のIMAP接続情報・Anthropic APIキーを記入
- `AWS_PROFILE=<env> python3 scripts/register-secret-parameters.py --env <env> --apply --expect-account <アカウントID>` を実行してSSMパラメータストアへ登録
- alphasyscdk PR #50・alphasystem PR #21のマージとCDKデプロイ
- デプロイ後、plan.md末尾「実行後の確認(結合確認)」項目5〜7(実環境でのポーリング動作・AI推測動作・`/sales/mail`系ページの動作確認)

**現在のステータス**: CDK側のコード対応(Draft PR #50・#21)まで完了。実値登録・デプロイ・結合確認はユーザー対応待ち。

## 2026-08-25 alphaaimail のアーカイブを決定

2026-08-19 の棚卸しで「後継の alphabsmail(Track1)に実質置き換わりつつあるが、正式な廃止/アーカイブの意思決定はまだ」としていた件を確定した。

- **決定**: alphaaimail は**アーカイブ済**とする。参照のみで改修しない。
- 経緯: デモ版として稼働していたが 2026-07-29 以降コミットが止まり、営業メール可視化(Track1)が alphadb / alphaexapi / alphasys 上で実装・マージされたことで役割を終えた。2026-07-27 の統合検討で「SQLite版はデモとして破棄。最初から社内システム基盤上に作り直す」と決めた方針どおりの帰結。
- `CLAUDE.md` の構成表を【デモ版・実質停止】から【アーカイブ済】に変更し、作業エージェントの「アプリ担当」から alphaaimail を外した。

**残る作業**: GitHub リポジトリ自体のアーカイブ設定(`gh repo archive`)は未実施。実施すると書き込み不可になるため、実行前に確認が必要。

### あわせて修正した構成表の不備

「Track1 は alphabsmail か」という確認から構成を調べた際、**`CLAUDE.md` の構成表に `alphabsmail` が載っていない**ことが判明した(2026-08-19 に当プロジェクト配下へ格納した際の更新漏れ)。新しいPCや別セッションで作業を始めたとき、構成表を見ても存在が分からない状態だった。

- 構成表に `alphabsmail` を追加。**独立リポジトリではなく alphasystem 配下のディレクトリ**である点を明記
- **「Track1 の実装場所に注意」節を新設**。`alphabsmail` にあるのは設計・計画で、実装コードは alphadb(スキーマ) / alphaexapi(IMAP取り込み・AI推測) / alphasys(画面) に入っている。さらにシークレット注入は alphasyscdk、実値の記入先は `alphaexapi/.env.{環境}` と分散しており、alphabsmail ディレクトリを見ても作業対象が見つからないため
- 作業エージェント節の `alphamail` という存在しない名前を `alphaaimail` に修正

**用語の注意として記録**: 「Track1」(営業メール可視化の第1段階)と「タスク1」(MFクラウド連携ロードマップの顧客管理連動)は別物。紛らわしいため、以降は「Track1」「MFタスク1」と書き分ける。

## 2026-08-25 Track1 のシークレット登録準備と、未使用 NEXTAUTH_SECRET の削除

### 危険な状態を作りかけた — プレースホルダ入りの .env を用意してはいけない

Track1 の登録作業を進めるにあたり、`alphaexapi/.env.staging` が存在しなかったため `.env.example` からコピーして雛形を用意した。**これが既存シークレットを破壊しかねない状態を作った。**

- 登録スクリプトは `.env.{環境}` → `.env` の順に読み、**値が空なら次のファイルへフォールバックする**(`if value:` による)
- しかし `.env.example` のプレースホルダは `<openssl rand -base64 32 で生成した値>` のように**空ではない**ため、そのまま値として読まれる
- `put-parameter` は `--overwrite` 付きのため、`--apply` すれば **SSM の正しい値がプレースホルダ文字列で上書きされ、既存機能が壊れる**

該当したのは `NEXTAUTH_SECRET` / `BANKCODE_API_KEY` / `CORPORATION_APPLICATION_ID` / `SHARED_DRIVE` の4件。dry-run 前に気づき、プレースホルダ値を空にして解消した(空にすれば `.env` へフォールバックし、無ければスキップされる)。

**教訓**: `.env` の雛形を用意する場合は、**値を空にした状態で作る**。`.env.example` をそのままコピーしてはいけない。

### NEXTAUTH_SECRET は未使用だったため3箇所から削除

`.env.example` の `NEXTAUTH_SECRET` の用途を確認したところ、**alphaexapi のどこでも使われていない**ことが判明した(`node_modules`・`dist` を除くリポジトリ全体を検索して `.env` 系ファイル以外に参照0件)。

- バックエンドの認証は `src/auth/google-auth.strategy.ts` による **Google IDトークン検証**で、Google の公開鍵で検証するため署名鍵を持つ必要がない
- バックエンドのひな形作成時にフロント側の設定をコピーした残骸と思われる
- フロント(alphasys)のセッション用 `AUTH_SECRET` とは別物。そちらは `src/lib/auth.ts` で実際に使われており、引き続き必要

使わないシークレットを SSM に保管しコンテナへ注入し続ける状態だったため、3箇所から削除した(alphasystem / alphaexapi #52 / alphasyscdk #52)。**3リポジトリはセットで反映する。CDK はマージだけでは反映されずデプロイが必要。**

### 判明した参照関係(迷いやすい点)

`GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` は **`alphaexapi/.env` に書いても SSM には登録されない**。登録スクリプトはこの2つを `alphasyscdk/.env` の `AUTH_GOOGLE_ID` / `AUTH_GOOGLE_SECRET` から読み、`/alpha/{環境}/shared/` に集約する(フロントとバックが同一の OAuth クライアントを共用するため)。直感に反するため `.env.example` に注記を追加した。

なお Track1 の5項目(`SALES_MAIL_IMAP_*` / `ANTHROPIC_API_KEY`)が必要なのは **alphaexapi のみ**。参照コードは `src/sales-mail/mail-ingestion.service.ts` の1ファイルで、ECS でも `backendSecrets` にのみ注入される。alphasys 側には不要。

### Track1 の残作業

`.env` への実値記入は完了。SSM への登録(dry-run → `--apply`)、alphasyscdk のデプロイ、結合確認が残っている。

## 2026-08-28 Track1 シークレットの staging 登録完了と、npm レジストリ設定の整理

### SSM 登録が `--apply` で全件失敗した — ドライランでは絶対に露見しない不具合

`register-secret-parameters.py` を staging に対して `--apply` したところ、**全件が `You must specify a region` で失敗**した。`aws ssm put-parameter` はリージョン必須だが、スクリプトが `--region` を渡していなかった。

**なぜドライランで気づけなかったか**: `put_parameter()` は `dry_run` のとき AWS を呼ばずに即座に `True` を返す作りになっている。つまり**ドライランは「値が読めるか」しか検証しておらず、AWS 呼び出し側の不備は `--apply` して初めて出る**。ドライランが通ったことを「登録できる」の根拠にしてはいけない。

**「`AWS_PROFILE` を環境変数で指定しているのに、なぜリージョンが必要なのか」**: `AWS_PROFILE` が解決するのは**認証情報**であって、リージョンは `~/.aws/config` の当該プロファイルに `region = ...` が書かれている場合にのみ一緒に解決される。書かれていなければリージョンだけ未解決のまま残る。今回がまさにその状態だった。

- `~/.aws/config` 側はユーザーが対応。
- スクリプト側も、**aws CLI の設定に依存せず単体で完結する**よう修正した(`AWS_REGION` / `AWS_DEFAULT_REGION` があればそれを使い、無ければ `ap-northeast-1`)。`put-parameter` と `sts get-caller-identity` の両方に渡す。
- alphasystem PR #31 → #32 で main まで反映済み。

修正後、**staging の SSM 登録は成功**。

### `~/.npmrc` をグローバルに置くのをやめ、alphadb を使うリポジトリだけに限定した

CodeArtifact のレジストリ設定を `~/.npmrc`(ユーザーグローバル)に置いていたため、**alphadb と無関係なプロジェクトの `npm install` まで CodeArtifact 経由になり、トークン切れのたびに全プロジェクトが道連れで失敗する**状態だった。ユーザーから「ことあるごとにエラーが出て非常に使いにくい」との指摘。

**整理した結果**:

- `~/.npmrc` は**削除**。グローバルには何も置かない。
- CodeArtifact の設定は**リポジトリ直下の `.npmrc` にのみ置く**。対象は alphadb パッケージを使う3リポジトリ: **alphadb / alphaexapi / alphasys**。
- **alphasyscdk / alphacdk / alphasystem / alphabatch には置かない**。これらは通常の npmjs から解決する。

**教訓**: 特定プロジェクト専用のレジストリ設定をユーザーグローバルに置くと、影響範囲が無関係なプロジェクト全体に広がる。プロジェクト単位の設定はプロジェクト単位のファイルに置く。

なお `~/.npmrc` が全プロジェクトに効いていた期間に `npm install` した `node_modules` は、CodeArtifact 経由で解決されている可能性がある。**alphasyscdk / alphacdk はクリーンインストールし直した**。

- 事前確認: 両リポジトリとも作業ツリーはクリーン。`package-lock.json` に codeartifact の URL は**0件**(ロックファイル自体は汚染されていなかった)。`node_modules` / `cdk.out` はいずれも `.gitignore` 済みで、削除しても git 上は無傷
- `.env` 系は `.gitignore` されており**消すと復元できない**ため対象外とした。削除したのは `node_modules` と `cdk.out` のみ
- `npm ci` 後、両リポジトリとも `npm config get registry` が `https://registry.npmjs.org/` を返すことを確認
- alphasyscdk は `npx cdk list` が成功し、`SharedStack` / `StagingStack` を出力(合成が通ることを確認済み)
- `npm ci` 時に esbuild の postinstall がスキップされる警告が出るが、`@esbuild/win32-x64` 由来の `esbuild.exe` が入っており `npx esbuild --version` も通るため実害なし

### Track1 の残作業(この時点)

| 項目 | 状態 |
|---|---|
| `alphaexapi/.env.{staging,prod}` への実値記入 | 完了 |
| staging の SSM 登録(`--apply`) | **完了** |
| prod の SSM 登録 | 未実施 |
| alphasyscdk の staging デプロイ(ECS への注入反映) | **未実施** ← 次はここ |
| 結合確認(plan.md 末尾 項目5〜7) | 未実施 |

alphasyscdk の `lib/constructs/ecs.ts` への Track1 5項目(`SALES_MAIL_IMAP_*` / `ANTHROPIC_API_KEY`)の追加は `staging` にマージ済みだが、**マージだけでは ECS タスク定義に反映されない。CDK デプロイが必要**。

未使用 `NEXTAUTH_SECRET` の削除(2026-08-25 起票)は、alphaexapi #52 / alphasyscdk #52 / alphasystem #29 の3本ともマージ完了。

## 2026-08-31 ステージング環境の作り直しと、そこで見つかった10件の不具合

**目的**: 「本番デプロイで極力エラーが出ないよう、ステージング構築を完璧にし、手作業部分も確定させたい」という方針を受け、StagingStack を削除して手順書どおりに作り直した。

**結果**: **10件の不具合が判明し、すべて修正した。いずれも本番でも同じように失敗していたもの。** 最終的に `running: 2` / `CREATE_COMPLETE` となり、画面表示まで確認できた。

### 判明した不具合と、その性質

| # | 内容 | なぜ事前に気づけなかったか |
| --- | --- | --- |
| 1 | buildspec の `echo` 未クォートで dash の構文エラー | **マイグレーションは一度も成功していなかった**。BUILD の1行目で落ちるため先に進まない |
| 2 | appUser のデッドロック | 「デプロイ後に手動で `CREATE USER`」が、デプロイが完了しないため成立しない |
| 3 | `AwsCustomResource` の応答が4096バイト超 | 環境変数を2件追加しただけで閾値を越えた |
| 4 | VPC 内 CodeBuild の ENI 残骸でロールバックが停止 | 削除を試して初めて遭遇する |
| 5 | **`DB_NAME` が空でマイグレーションが `postgres` DB へ** | **デプロイは成功する**。最も発見しにくい |
| 6 | ECR タグ immutable で同一バージョンを作り直せない | **ビルドは「成功」と表示され push だけ失敗する** |
| 7 | `js-yaml` が未宣言 | builder では推移的依存で解決でき、**本番イメージでのみ落ちる** |
| 8 | Node 18 にグローバル `crypto` が無い | Track1 の `@Cron` が初めて動いて表面化 |
| 9 | Alpine と `binaryTargets` の不一致、`slim` に OpenSSL 無し | alphaexapi だけベース環境がずれていた |
| 10 | Next.js のバンドルで Prisma エンジンが見つからない | 実行時に初めて出る |

詳細は [alphasyscdk/docs/構築・メンテナンス手順.md](../../alphasyscdk/docs/構築・メンテナンス手順.md) の付録B に、症状・原因・対処の表として記録した。

### 構造的な学び

- **「デプロイ後に手動で行う」手順は、デプロイの完了条件に関わる作業には使えない**（#2）。ECS の安定を待つ CloudFormation は、アプリが動かない限り完了しない。手順ではなくコードで解決すべきだった。
- **成功したように見える失敗が3件あった**（#5 デプロイ成功・#6 ビルド成功・#7 ビルド成功）。ログの終了ステータスだけを見ると見逃す。
- **イメージのベース環境は3リポジトリで揃える必要がある**（#9）。alphadb の `binaryTargets`・alphasys・alphaexapi の Dockerfile がずれていたことが後半の原因。
- **ECS はマイグレーションの完了を待たない**ため、失敗すると3時間のタイムアウトまで原因が分からない。デプロイ開始直後に成否を確認する手順を新設した（手順書 2-4-3）。

### 手作業として残ったもの / 無くしたもの

- **無くした**: appUser の `CREATE USER`/`GRANT`（マイグレーション CodeBuild に統合し冪等化）
- **残る**: GitHub 接続の承認（AWS 仕様で自動化不可）、Parameter Store への機密登録（スクリプトあり）、alphadb の publish 起動

### ECR タグの運用（決定）

**通常運用では版数を上げる。初期構築・デバッグ中のみ一時的に mutable にし、必ず戻す。**

immutable はこの構成の要で、「ステージングで検証したイメージ」と「本番が動かすイメージ」の同一性がタグの不変性に依存している。恒久的に mutable にしてはいけない。

### 本番構築に向けて残っている前提

- **本番アカウントに `alphacmc.com` のホストゾーンが無い**（C-1。未解決のまま）
- **本番アカウントにも alphacdk の NetworkStack が必要**。`ecs.ts` が `NetworkStack-*` の export を `importValue` しており、手順書 0-2 の「クロススタック参照なし」は誤りだった

## 2026-09-01 現行システムからのデータ移行（ステージングで完走）

ステージング環境の構築が完了しDBが空だったため、現行システムからのデータ移行を実施した。
**全14テーブル・約15万件の投入と整合検証まで完走した。**

### 前提が資料と食い違っていた（2件）

**① 移行元の方式**

資料は「`10.0.3.154`（現行のバックアップDB）へ直接接続」と書かれていたが、**実際の方針は
「日次ダンプをローカルのコンテナへリストアして抽出」**だった。以前の会話で決めていた内容が
資料に残っておらず、危うく稼働中のDBへ直接接続するところだった。

ダンプ方式の利点は、稼働中DBに接続を持ち込まない・**同じダンプから何度でもやり直せる**・
ネットワーク到達性に依存しない、の3点。実際に抽出SQLの修正を4回繰り返したため、
この利点が効いた。

**② 移行元テーブル（D4）**

資料は「移行元は新形 `alp_users`(118件)、旧 `alp_user` は対象外」としていたが、
**ダンプに `alp_users` は存在しなかった**。`alp_users` は調査に使った `10.0.3.154` 固有の
もので、現行システムには無い。旧形 `alp_user`(120件)からの変換として D4 を設計し直した。

### D4 で確定した判断

- **全120件を移行**（退職62件を含む）。過去の契約・原価が退職者を参照するため
- 欠損は既定値で埋める。`email` は UNIQUE のため `retired-<scode>@invalid` で一意化
  （`.invalid` は到達不能な予約TLD）、日付は `1900-01-01` を番兵値に
- `auth` 0,1→USER / 2,4,8→ADMIN、`title` 0〜5 は enum 定義順、**`sex` は 1=男 / 0=女**
- **マイナンバー(`my_number_info`)とパスワードは意図的に移行しない**

> **`sex` は当初「0=MALE」と仮定して資料に書いていた。実際は逆。**
> 確認せずに実装していれば120件全員の性別が反転していた。
> 仮定のまま進めず、現行システムで確認してから実装したことが効いた。

### FK整合のために除外したもの

| 対象 | 件数 | 理由 |
| --- | --- | --- |
| 契約 | 10 | 参照先が発注PJで移行対象外。受注5/発注5のペア、すべて2017年6月以前に終了・金額空欄 |
| 請求 | 12 | 上記契約を参照するため |
| 原価 | 6 | `scode=133` が社員マスタに存在しない（2024年10月〜2025年3月の固定給） |

**学び**: 契約を除外した際、それを参照する請求を残したまま投入して失敗し、
`bill_recipt` 以降が1件も入らなかった。**除外の判断をしたら下流の参照も必ず追うこと。**
残りのテーブルについては事前に孤児参照の有無を全件確認し、以降は失敗しなかった。

### 実行手順として確定したもの

```
S3のダンプ → ローカルコンテナ(postgres:16, 5433) → extract.ps1 → data/*.csv
                                                                      ↓
                          SSMポートフォワード(15432) → load.ps1 → ステージングDB → verify.sql
```

詰まった点はすべて手順化した（→ [alphadb overview.md 5-2](../../alphadb/prisma/doc/migration/overview.md)）。

- **Session Manager プラグイン**が前提ツール（AWS CLI とは別。PCごとに必要）
- **シークレットの「名前」はユーザー名ではない**。JSON の中身から取り出す
- **`-DbHost localhost` は動かない**。psql が Docker 内で動くため `host.docker.internal`
- 投入は**マスター**で行う（appUser は `setval` を実行できない）

`verify.sql` を新設し、件数・FK整合・enum変換・既定値・シーケンスの5観点を
**本番でも同じ検証を繰り返せる**形にした。

### 認証が通らなかった件（設定ミス）

投入後に画面を確認しようとしたところ、Google 認証が `redirect_uri_mismatch` で失敗した。
`.env.staging` の `AUTH_URL` が **`alphasystem`**.alphacmc-test.com（プロジェクト名）に
なっており、正しくは **`alphasys`** だった。`.env.staging.example` は正しかったため、
手元でファイルを作成した際の混入と考えられる。**`.env.prod` も本番構築前に要確認。**

### 残タスク

1. **画面での実データ確認** — SQL上の整合とアプリの期待が一致するかは別問題
2. **D3: `alp_members` の初期データ設計** — 移行元が無い唯一の未着手項目
3. **本番での実施** — 確定した手順をそのまま適用する

## 2026-09-02 Track1 メール取り込みが初めて成立するまで

ステージングで営業メールの取り込みが**全件失敗**していた。原因は独立した3つで、
順に潰して **297件の保存**まで到達した。

### 原因1: アプリが古い alphadb に固定されていた

`Null constraint violation on the fields: (updated_at)` が全件で発生。

8/13 に alphadb で3つのコミットが**同日に**入っていた。

| 順序 | 内容 |
| --- | --- |
| 1 | **1.1.0 リリース** — Track1 スキーマだが `updatedAt` なし |
| 2 | `updatedAt` を Track1 テーブルに追加 |
| 3 | **1.2.0 リリース** — 上記の修正込み |

alphaexapi の lockfile は **1.1.0 のまま**で、1.2.0 に上げられていなかった。
一方 DB マイグレーションは `updatedAt` ありのスキーマから適用され、
`updated_at` は NOT NULL で作られている。

**`@updatedAt` はクライアント側で値を設定する機能**のため、クライアントが
カラムを知らなければ INSERT に含まれず、DB の制約で拒否される。
**スキーマを変えたら、利用側の版数を上げるまでが1セット。**

### 原因2: 失敗したメールを既読にしていた（設計上の欠陥）

保存の成否に関わらず UID を既読対象に入れていた。取り込み対象の選択は
`{seen: false}` なので、**一度既読にされたメールは二度と取り込まれない**。

原因1で298通すべてが失敗し、そのまま既読にされ、**DB 0件・IMAP 全件既読**
という取り残しが生まれていた。「失敗はログに出るから気付ける」つもりが、
実際には**取り込み対象がゼロになって静かに止まる**状態だった。

既読にするのは「再取得しても結果が変わらない」ものだけに限定した
（→ alphaexapi PR #63）。

| 結果 | 既読にするか | 理由 |
| --- | --- | --- |
| 保存成功・重複 | する | 再取得不要 |
| Message-ID / From なし | する | 再取得しても取り込めない。未読だと無限に再試行する |
| 例外で失敗 | **しない** | 復旧しうる原因が多い。次回に再試行させる |

**学び**: 外部システムの状態を「処理済み」に変える操作は、**成功した対象だけ**に
限定する。失敗を含めると再試行の道が閉じ、データ損失が静かに起きる。

既存テスト2件が旧挙動（失敗も既読にする）を**明示的に固定していた**ため、
期待値も更新した。テストが仕様として誤りを固定していた例。

### 原因3: 初回リリース前なのに版数が 1.2.0 まで上がっていた

一度もリリースしていないため 1.0.0 の上書き運用に戻した（→ alphadb PR #40/#41）。

8/6 に入れた上書き publish の仕組みは、**8/13 に意図的に撤回**されていた
（`ci: switch to plain npm publish, drop version-overwrite workaround`）。
それを知らずに「削除処理が消えている」と誤って判断した。
**履歴を確認せずに原因を決めつけない。**

CodeArtifact の 1.1.0 / 1.2.0 を削除して 1.0.0 のみ残したため、両アプリの
`^1.1.0` / `^1.2.0` が解決不能になり `ETARGET` が発生した。
上書き運用の間は**キャレットなしの固定指定**にした（`^1.0.0` だと将来
1.1.0 を publish した時点で意図せず追随する）。

> **初回リリース時に、buildspec の削除ブロックを必ず外すこと。**
> リリース済みバージョンを CI が消せる状態を残してはならない。

### 取り残し298通の回復

既読フラグを外して再取り込みさせた。`messageId` の一意制約があるため
重複登録は起きない。

**imapflow で UID の配列を渡すと `resolveRange` が `join(',')` で連結するだけ**で、
`packMessageRange` による圧縮は効かない。検索条件オブジェクト（`{seen: true}`）を
渡すと SEARCH 経由で `1:299` のように圧縮される。

`messageFlagsRemove` は**例外を投げずに `false` を返す**ことがあり、戻り値を
見ていなかったため「298件を未読化した」と表示しながら実際には何も変わって
いなかった。**成否を戻り値で返す API は必ず確認する。**

### CI/CD の自動化範囲（確認結果）

| 対象 | 自動起動 |
| --- | --- |
| alphasys / alphaexapi のイメージビルド | **する**（`staging` への push で webhook 起動） |
| alphadb の publish | しない（手動 `start-build`。デフォルトブランチ `main` をビルド） |
| ECS デプロイ | **しない** |

イメージタグは `package.json` の version から決まるため、**依存だけ変えた場合は
同じタグに push される**。ECS は変化を検知しないので `--force-new-deployment` が必須。

### 未解決だった3点（同日に解消）

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | 失敗2件（uid=175, 272） | **解消**。原因は NUL バイト（下記） |
| 2 | `@Cron` の重複実行 | **解消**。アドバイザリロックで排他（下記） |
| 3 | `Invalid uidset` | **未解決**。再現せず原因未特定 |

3 は 297件のUIDをカンマ連結した STORE が成功しており再現しない。**原因未特定のまま
推測で直すことはしない**方針とした。ログ改善により再発時は追跡できる。

### 原因4: メールに NUL バイトが含まれていた

uid=175/272 の2通が必ず保存に失敗していた。ログを改善して初めて原因が見えた。

```
PostgresError { code: "22021",
  message: "invalid byte sequence for encoding \"UTF8\": 0x00" }
```

**PostgreSQL の text / varchar は 0x00 を格納できない。** パースは成功し
`create()` の実行時に拒否される。壊れた添付やエンコード宣言の誤りで発生し、
**送信側の都合なので受信側では避けられない。**

DBへ渡す文字列すべてから 0x00 を除去する方針とした（→ alphaexapi PR #68）。
どのフィールドに混入するかは送信側次第のため一律に適用する。改行やタブは
PostgreSQL に格納できるため触らない。

> **判断**: メール1通が丸ごと取り込めないことと、本文から制御文字が失われること
> を比べ、後者を選んだ。営業メールの可視化という目的からして軽微。

**学び**: 「パースは通るのに保存だけが必ず失敗する」類型は、DB の型が受け付け
ない値が混入している可能性を先に疑う。アプリのロジックを追っても出てこない。

### CloudWatch は「1行 = 1イベント」

**ログ設計上の制約として記録する。**

コンテナの標準出力は行単位でイベント化されるため、**改行を含むメッセージは複数
イベントに分割される**。`--filter-pattern` で検索できるのは1行目だけになり、
Prisma のような複数行エラーでは `code` / `meta` / スタックが別イベントに散る。

実際に `--filter-pattern "uid=175"` では次の1行しか取れず、原因が見えなかった。

```
メッセージの取り込みに失敗した（uid=175, 段階=保存, 7861バイト）: PrismaClientUnknownRequestError:
```

時刻範囲を指定して分割されたイベントを手作業でつなぎ、ようやく `22021` を確認できた。

**例外をログに出す際は1行に畳む。** 行ごとに trim して空行を落とし、区切り文字で
連結する（→ alphaexapi PR #68）。単純に改行を置換するだけでは、**空白だけの
メッセージが区切り文字になって「中身があるように見える」**（`Error: | at ...`）。

### 原因2の対処: 接続数1の PrismaClient でアドバイザリロック

`@Cron` は `@nestjs/schedule` が**プロセス内に持つスケジューラ**で、各プロセスが
自分の時計で発火する。`desiredCount: 2` なので同じ時刻に2回発火していた。
既存の `isRunning` はプロセス内変数のため別プロセスには効かない。

共有DBの `pg_try_advisory_lock` で排他した（→ alphaexapi PR #65）。
待たない `try` 版を使う。「他が動いているなら今回は諦める」が正しく、次の
ポーリングで再試行すれば足りる。待つ版だと全タスクが順番待ちして同じ処理を繰り返す。

**ロック専用に接続数1の PrismaClient を持つ。** ここが設計の要点。

`pg_try_advisory_lock` は**ロックを接続に結びつける**。通常の接続プール経由だと
取得と解放で別の接続が割り当てられうる。そうなると `pg_advisory_unlock` は
「保持していないロック」の解放となって失敗し、**ロックが接続の再利用まで残って
以降の取り込みが全てスキップされる**。しかも「たまたま動く」ことが多く質が悪い。

> **`connection_limit=1` を増やしてはならない。** 効率が悪いように見えるが、
> 増やすとこの問題が再発する。コードにも明記した。

トランザクション版（`pg_try_advisory_xact_lock`）は採らなかった。ロック保持には
取り込み全体をトランザクションで囲む必要があり、IMAP のダウンロードと AI 推測
（外部API）を含む数分の処理で `idle in transaction` が続くため。

EventBridge 方式（RunTask / controller）も検討したが、**「10分ごとに1回起動する」
ことは保証しても「前回の実行中か」は見ない**ため、処理が10分を超えると同じ問題が
別の形で再発する。インフラ変更と費用に見合わないと判断した。

### 最終結果

| 項目 | 値 |
| --- | --- |
| INBOX | 299通 |
| `alp_sales_email` | **299件**（取り込み成功） |
| 失敗 | 0件 |

本日マージした PR: alphadb #40/#41、alphasys #142、alphaexapi #61/#63/#65/#66/#68。

### 誤って断定したこと（記録）

- 「公開済みクライアントが古い」 → 実際は**アプリ側の版数固定**が原因
- 「fetch が `BODY[]` で暗黙に既読化する」 → imapflow は `BODY.PEEK[]` を使う
- 「`Invalid uidset` は uidset が長すぎるため」 → 297件の連結で成功しており未確定
- 「`main` は 1.2.0 のまま」 → `git fetch` せず古いローカル参照で判断していた

いずれも**現物を確認する前に説明を組み立てた**ことが原因。

### 作業上のつまずき（同種の再発防止）

- **`git fetch` 前の `origin/staging` からブランチを切った。** 直前にマージされた
  PR を含まない基点で編集し、パッチの適用に失敗して気付いた。同じ理由で別のPRも
  古い基点になっていた。**ブランチを切る前に必ず fetch する。**
- **バックスラッシュを含む編集が繰り返し失敗した。** ツール経由でエスケープが
  半減し、`
` が実際の改行に化けて照合できなかった。正規表現や制御文字を含む
  コードを生成する際は、**文字コードから動的に組み立てる**か、既存箇所を避けた
  照合条件にする。
- **`messageFlagsRemove` の戻り値を見ていなかった。** 例外を投げず `false` を
  返す API で、「298件を未読化した」と表示しながら実際には何も変わっていなかった。
  **成否を戻り値で返す API は必ず確認する。**
- **テストが旧挙動を仕様として固定していた。** 失敗メールを既読にする既存テストが
  2件あり、修正時に期待値の更新が必要だった。逆に、今回追加したテストは私の実装
  不具合を2件検出した（空白だけのメッセージ、スタックの接頭辞二重）。

---

## 2026-09-03 営業メール確認キューの設計と実装

取り込んだ299件が**全件「取引先未確定」で、確認キューから何も操作できない**
状態だった。ここから、人がメールを処理できる状態までを作った。

### `/sales/mail` が空だったのは仕様どおり

この画面は `MATCHED` / `REVIEWED_NO_CONTRACT` のメールを持つ顧客だけを表示する。
全件 `UNMATCHED` なら0件になる。**不具合ではない。**

行き詰まっていたのは確認キューの側で、契約の候補は `customerNo` から引くため、
**取引先が決まっていないメールは何も操作できなかった**。

### 確定した動線

```
UNMATCHED       取引先を選ぶ        → NEEDS_REVIEW
NEEDS_REVIEW    契約を選ぶ(複数可)  → MATCHED           → /sales/mail に表示
                どれにも該当しない  → REVIEWED_NO_CONTRACT
いずれの状態でも 無関係として削除    → DBとメールサーバーから完全削除
```

設計書の3段階に実装を合わせた。それまでは**ドメイン一致した時点で `MATCHED`**
にしており、`NEEDS_REVIEW` を一度も設定していなかった。人が指定した場合と
ドメイン一致の場合で状態が割れるため揃えた（→ alphaexapi PR #70）。

型を `'NEEDS_REVIEW' | 'UNMATCHED'` に狭めたことで、AI契約推測の起動条件
（`if (matchResult === 'MATCHED')`）をコンパイラが検出した。**狭めていなければ
推測が静かに動かなくなっていた。**

### SaaS ドメイン問題（設計の前提を突く指摘）

受発注や請求に **SaaS を使う取引先が複数あり、差出人が SaaS 事業者のドメインに
なる**。しかも同じ SaaS を複数の取引先が使う。

`alp_customer_domain.domain` は一意制約を持つため、SaaS ドメインを登録すると
**その SaaS を使う他の取引先のメールまで1社に誤って紐づく**。未確定のまま残るより
悪い状態になる。

一方、**件名や本文に会社名・契約名が入っているケースがあり、人が見れば判断できる**。

**確定した方針**: 操作を2つに分ける。

| 操作 | 対応表への影響 | 用途 |
| --- | --- | --- |
| 取引先を選ぶだけ | **なし** | **SaaS 経由**（既定の動線） |
| チェックを入れて確定 | ドメインを登録 | 取引先の自社ドメイン |

チェックボックスは**既定でオフ**。両画面に理由を明記した。

**学び**: 「ドメインで顧客を特定する」という前提自体が、SaaS の普及で崩れていた。
自動化の前提が業務実態と合っているかは、実データを見るまで分からない。

### ドメイン登録の口が存在しなかった

`alp_customer_domain` にデータを入れる手段が**alphasys にも alphaexapi にも
移行データにも無かった**。自動マッチングはこの表を引くだけなので、空である限り
全件 `UNMATCHED` になる。顧客管理画面と確認キューの両方に登録経路を作った。

誤登録を防ぐ検証を入れた。

- **自社ドメインの登録を禁止**（送信メールでは差出人が自社になるため、相手先
  ドメインは宛先側から導出する）
- **他社に登録済みなら取引先名を出して拒否**（一意制約で黙って失敗すると原因を
  追えない）
- `@` 付き・アドレス貼り付け・ドット無しを拒否し、小文字に正規化
  （マッチング側は小文字で引くため）

### 画面の絞り込み（実運用での指摘を反映）

- **取引停止（`sleep`）の取引先を選択肢から除く**
- **略称（`customerNick`）で表示**。正式名称は長く見分けにくい。未登録なら正式名称
- **プルダウン形式の絞り込みコンボ**。244件を並べても探せない。候補は入力欄に
  重ねて表示し、明細の高さを変えない
- **契約候補から終了済みを除く**。`contTo` 未設定は継続中とみなして含める

> 候補の選択は `onMouseDown` で行う。`onClick` だと入力欄の `blur` が先に走って
> 一覧が閉じ、クリックが届かない。

### 無関係メールの削除

取り込み専用メールボックスには**営業と関係のないメールも大量に届く**。確認キューに
残ると確認すべきメールが埋もれる。

**DBとメールサーバーの両方から完全削除する。** DBだけ消すと既読のままサーバーに
残り続けるため。**原本は各担当者のメールボックスにも配信されており、必要なら
再転送できる**（取り込み専用メールボックスは fan-out されたコピーの1つ）。この
前提があるため、取り違えても復旧可能と判断した。

`SalesEmail` は IMAP の UID を保持していないため **Message-ID で引く**。同じ
Message-ID は定義上同一のメールなので、複数一致してもまとめて削除してよい。

サーバー側の削除に失敗してもDBの削除は続行する。確認キューから消せない方が実害が
大きく、サーバーに残ったメールは既読のため再取り込みもされない。

### 削除したメールからの学習 → 同日に実装（差出人単位の除外ルール）

当初「ベース完成後の課題」としたが、**削除がユーザ処理の大半を占める**ことが
判明したため同日に着手した。

### PR の取り込み漏れ（段取りの失敗）

alphasys #146 の base を `feature/select-contracts-in-review` のままマージしたため、
**`staging` に入らなかった**。#145 は既にマージ済みで、#146 の変更（ドメイン登録の
11ファイル）はどこにも反映されず、機能が丸ごと欠落した状態でデプロイされた。

PR 本文に「マージ後に base を切り替えてください」と書いただけで、切り替え自体を
確実にしていなかったのが原因。

**再発防止**: PR は積まない。先行PRがマージされてから後続ブランチを切り直す。
やむを得ず積む場合は、先行PRのマージ直後に `gh pr edit <番号> --base staging` で
base を切り替える。

### 既知の未対応

**`CustomerSearchModal` のテスト10件が失敗している。** 一連の変更より前から失敗して
おり、変更を退避した状態で確認済み。3画面に**重複コピー**されているコンポーネントで、
今回の確認キューでは使っていない（別途 `CustomerCombo` を新設した）。

---

## 2026-09-03 無関係メールの自動除外（差出人単位の除外ルール）

確認キューが機能し始めた結果、**無関係メールの削除が確認作業の大半を占める**
ことが分かった。同じ差出人から繰り返し届くものにも毎回対応が必要だった。

### 検討した方式

| 方式 | 判定 |
| --- | --- |
| **差出人単位の除外ルール** | **採用。**確実で誤判定がほぼ無い。定期配信・通知に強い |
| 件名のパターン | 見送り。取引先の正当なメールが同じ語を含むと誤って消える |
| AI 分類 | 見送り。自動削除に使うと誤判定に気づけない。将来の候補提示用 |
| 一括削除（差出人でまとめる） | 段階1として提案したが、除外ルールを先に入れたため不要になった |

**AI を自動削除に使わない**のが方針上の要点。誤判定で取引先のメールが消えても
気づけないため、AI は「候補提示」までに留める。

### 確認は登録時の1回だけ

```
1回目: 削除するとき「今後この差出人も除外する」を選ぶ（ここで確認）
2回目以降: 取り込み時に自動で除外。確認キューにも出ない
```

**除外対象はメールサーバーからも削除する。** DBに保存しないだけでは既読のまま
溜まり続け、無関係なメールがメールボックスを占め続ける。

### 除外の単位を2つに分けた理由

| 単位 | 判定 | 意図 |
| --- | --- | --- |
| `ADDRESS`（既定） | 完全一致 | **同じドメインの別アドレスには広がらない。**取引先の担当者からのメールを残す |
| `DOMAIN` | `@` 以降が一致 | 効果は大きいが、同じドメインの商談も消える |

**アドレス単位が同じドメインに波及しないことをテストで固定した。** ここが単位を
分けた意図そのものであり、実装が崩れると静かにメールが失われる。

ドメイン単位は取引先の対応表（`alp_customer_domain`）と衝突する余地があるため、
既定にはせず明示的に選ぶ形にした。画面でも警告を出す。

### 誤登録に「後から気づける」設計

自動削除は都度の確認が無いため、**誤ったルールを登録すると以後黙って消え続け、
気づく機会がない。** これが方式の唯一の弱点であり、対処を設計に組み込んだ。

| 手段 | 内容 |
| --- | --- |
| `matched_count` | 除外件数。**想定より多く消していないか**が分かる |
| `last_matched_at` | ルールがまだ効いているか分かる |
| `note` | 登録の契機になったメールの件名。何を除外したのか辿れる |
| 管理画面 | 除外件数の**多い順**に並べ、ルールを削除できる |
| 取り込みログ | `除外12件` のように件数を出す |

**都度確認の代わりに、後から気づける仕組み**という位置づけ。操作の手間を増やさず
誤登録を検出できる。

### 実装上の判断

- **判定はパース後**に行う。差出人アドレスを取り出す必要があるため
- **ルールの登録は削除より先**に行う。削除だけ成功してルールが残らないと、次の
  ポーリングで同じ差出人のメールがまた入ってくる（順序をテストで固定）
- **登録は `upsert`**。`pattern` に一意制約があり、二重登録を弾くだけでは
  「登録できない」と見えて原因が分からない
- **表記は小文字に揃える**。メールアドレスの大文字小文字は区別されない運用が
  一般的で、揃えないと同じ差出人が一致しなくなる（`alp_customer_domain` と同じ方針）
- ルールを削除すれば**次のポーリングから効かなくなる**（取り込みはルールを毎回
  読み直す）。既に削除されたメールは戻らない

### 学習データの保持は不要だった

当初「削除前に判定材料を残す設計が必要」と考えたが、**除外ルールそのものが残る**
ため履歴の保持は要らなかった。

履歴が必要になるのは**AIに判断例を与える段階**だけ。その時点で「削除時に差出人と
件名を記録する」を追加すれば足り、本文まで残す必要はない。

### 原本が失われる点について

**メールサーバーからも完全削除するため、取り違えると原本が失われる。** ただし
取り込み専用メールボックスは fan-out されたコピーの1つで、原本は各担当者の
メールボックスにも配信されており再転送できる。この前提があるため、確認ダイアログ
以上の安全策は設けないと判断した。

### 変更したリポジトリ

| リポジトリ | 内容 |
| --- | --- |
| alphadb | `alp_excluded_sender` 追加（PR #42） |
| alphaexapi | 取り込み時の除外判定、ルールAPI（PR #74） |
| alphasys | 削除時の選択、管理画面（PR #158） |

スキーマ追加のため、**publish に加えてマイグレーションの適用が必要**だった
（→ [alphasyscdk 手順書 ゲート②-1](../../alphasyscdk/docs/構築・メンテナンス手順.md)）。
